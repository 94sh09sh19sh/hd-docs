# 血液透析平板照護輔助系統 — 完整手動測試手冊

> 從 `git clone` 一路走到每一個細節功能。每一項都寫明「怎麼做」與「應該看到什麼」。
> 對應版本：迭代 6 · 107 條 API 路由 · 68 種稽核動作。
> 資料庫已於 2026-09-10 改為本機 SQLite，建置步驟已隨之更新。
> **本頁是主手冊**：環境建置、兩條主線、離線韌性、迭代 1／2 的細節功能、收工與附錄。迭代 3 之後每個迭代各有一本分冊，見 [§13 分冊索引](#13--迭代-3-6-分冊)。

本頁的核取方塊在 GitHub 上可直接勾選，用來記錄你走到哪裡；文件站上的是唯讀的，勾不動。

## 操作端色籤

| 標記 | 代表你要在哪一端操作 |
|---|---|
| **護理端** | 護理站主控台（瀏覽器） |
| **病人端** | 病人端平板（瀏覽器） |
| **終端機** | 終端機／命令列 |
| **資料庫** | 資料庫（Prisma Studio） |

---

## 00 · 開始之前

這份手冊假設你手上有一台 Windows 或 macOS 開發機與 Chrome。資料庫是一個放在專案目錄外的 SQLite 檔案，全程只在本機執行，不需要任何資料庫伺服器或雲端服務。

| 服務 | 網址 | 說明 |
|---|---|---|
| 護理站主控台 | `http://localhost:5173` | 護理端 PWA，需登入 |
| 病人端平板 | `http://localhost:5174` | Kiosk 介面，**沒有登入**，靠裝置金鑰佈建 |
| 後端 API | `http://localhost:3000/api` | 所有資料存取的唯一入口 |

> **注意｜測試資料規範**
>
> 《資料庫使用規範》第 10 條：**絕對禁止**輸入任何接近真實病人的資訊。手冊中所有姓名、病歷號皆為虛構，請照抄或自行編造。

每個測試項目左側的色籤標示你要在**哪一端**操作：

> **說明｜關於速度**
>
> 資料庫已於 2026-09-10 由雲端（Supabase，東京）改為**開發者本機的 SQLite**，Supabase 專案已於 2026-09-11 刪除。單次資料庫查詢往返約 **2 ms**，端到端（平板送出 → 護理端收到）約 **10 ms**，畫面操作幾乎沒有等待。
> 若你看到單一動作卡超過 1 秒，那多半是別的問題，可以往 §10 疑難排解找。

## 01 · 從 clone 到跑起來

- [ ] `a1` **［終端機］** Clone 專案並進入目錄。
  → 取得 `apps/`、`packages/`、`demand_v1/` 三個資料夾。

```bash
git clone https://github.com/94sh09sh19sh/hd-tablet-care.git
cd hd-tablet-care
```

- [ ] `a2` **［終端機］** 確認 Node 版本 ≥ 20.11。
  → `node -v` 顯示 v20.11 以上（開發環境用 v24）。版本太舊會在後續步驟出現語法錯誤。
- [ ] `a3` **［終端機］** 安裝相依套件。
  → npm workspaces 會一次裝好 api、兩個 PWA 與 shared 套件，過程無紅字錯誤。

```bash
node -v
npm install
```

> **注意｜npm install 出現權限錯誤（EPERM／EACCES／operation not permitted）**
>
> Windows 上最常見的三個原因，依序排查：**①** 有 node 行程正抓著 `node_modules` — 關掉還在跑的 dev server 與編輯器再重試。**②** 專案放在 **OneDrive／雲端同步資料夾**底下，同步程式會鎖檔 — 把專案移到不同步的路徑。**③** 防毒即時掃描擋住寫入 — 把專案資料夾加入排除清單。
> 都試過還是不行，再**以系統管理員身分**開啟 PowerShell 重跑；若已經裝壞了，先刪掉 `node_modules` 資料夾，執行 `npm cache clean --force` 後重來。

### 建立 .env

- [ ] `a4` **［終端機］** 複製環境變數範本。
  → 專案根目錄出現 `.env`（已列入 .gitignore，不會被提交）。

```bash
cp .env.example .env
# PowerShell：Copy-Item .env.example .env
```

> **說明｜資料庫誰來建？你只要決定它放哪裡**
>
> 這一步要填的 `DATABASE_URL` 是「**我希望資料庫檔案出現在哪裡**」，不是「請填入現有資料庫的位置」。
> 本系統的資料庫是**一個 SQLite 檔案**，沒有資料庫伺服器、沒有帳號密碼、不必事先安裝或建立任何東西。
> **資料夾與檔案都不必先建**：下一小節的 `npm run prisma:migrate`（`a7`）會依你填的路徑，把中間缺的資料夾與 `hd.db` 一起建出來。
> 所以填完 `.env` 時磁碟上還看不到任何東西，是**正常的** — `a13` 會回頭確認它真的生出來了。
>
> 順序只有一條規則：**先 `prisma:migrate`，再 `npm run dev`。** 跳過 migrate 直接啟動後端，
> 後端不會幫你建檔案，只會噴 `Error code 14: Unable to open the database file`。

以下兩個變數是後端的**硬性必填**（缺了直接拒絕啟動），`SUPER_ADMIN_*` 兩個照本手冊走也等於必填，
`BACKUP_DIR` 強烈建議一併填，其餘留空即用預設值：

| 變數 | 怎麼取得 |
|---|---|
| `DATABASE_URL`（**硬性必填**） | SQLite 連線字串：`file:` 加**絕對路徑**，例如 `file:D:/hd-data/hd.db`。路徑必須在**專案目錄外**的本機磁碟（不得為網路磁碟或 OneDrive 等同步資料夾），否則後端與 seed 會拒絕啟動。**資料夾與檔案都不必事先建立**，`a7` 的 migration 會一起建出來 |
| `JWT_SECRET`（**硬性必填**） | 自行產生，見下方指令 |
| `SUPER_ADMIN_WORK_ID` | 初始最高權限帳號的工作ID，例如 `ADMIN001`（英數字、底線、連字號，3–32 字元）。**這兩個留空後端照樣起得來，但資料庫裡不會有任何帳號，護理端登不進去** |
| `SUPER_ADMIN_INITIAL_PASSWORD` | 至少 8 字元。**首次登入後會被強制變更** |
| `BACKUP_DIR`（建議） | 備份輸出目錄，同樣須在專案目錄外的本機磁碟，例如 `D:/hd-data/backups`，目錄不必先建。未填時無法備份：§12 的備份還原演練、[迭代 3 分冊](iteration-3.md) §3.8、`verify:iteration3` 與 `verify:iteration4` 都會卡住 |

*在終端機執行 — 這是指令，不是要貼進 .env 的值*

```bash
node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
```

> **注意｜這一行是指令，不是設定值**
>
> 把**整行貼到終端機執行**（PowerShell、cmd、Git Bash 都可以），它會印出一串長長的亂碼。**那串印出來的東西**才是要填進 .env 的值：
> `JWT_SECRET=印出來的那一串`
> 不要把 `node -e ...` 這行本身貼進 `.env` — 那樣後端啟動時會把整行當成金鑰，雖然不會壞掉，但等於用了一把人人都猜得到的鑰匙。

> **地雷｜常見地雷**
>
> `DATABASE_URL` 一定要用**絕對路徑**。寫成 `file:./dev.db` 這類相對路徑，後端會直接拒絕啟動——相對路徑會依 `schema.prisma` 所在位置解析，很容易落在專案目錄內，一不小心就把資料庫檔案提交進版控。

- [ ] `a11` **［終端機］** 決定資料庫檔案要放哪裡，把絕對路徑填進 `DATABASE_URL`。
  → 挑一個**專案目錄外**的本機路徑即可，例如 `D:\hd-data\hd.db`，`.env` 就寫成 `DATABASE_URL=file:D:/hd-data/hd.db`（路徑用正斜線 `/`，含空白時整串加引號）。**現在不必去建那個資料夾，也不必建檔案**；此刻 `D:\hd-data` 還不存在是正常的。
- [ ] `a12` **［終端機］** 順手把 `BACKUP_DIR` 也填掉，例如 `BACKUP_DIR=D:/hd-data/backups`。
  → 同樣不必先建目錄（第一次備份時自動建立）。現在跳過，等走到 §12 或迭代 3 分冊 §3.8 時得回頭改 `.env` 並重新啟動後端。
- [ ] `a5` **［終端機］** 填完 `.env` 後，確認上表的五個變數都有值。
  → `DATABASE_URL` 以 `file:` 開頭、後面是專案目錄外的絕對路徑；`JWT_SECRET` 是一串亂碼而不是 `node -e ...`；`SUPER_ADMIN_WORK_ID` 與 `SUPER_ADMIN_INITIAL_PASSWORD` 都有填。

### 建立資料庫與測試資料

- [ ] `a6` **［終端機］** 先編譯共用型別套件（後端與前端都依賴它）。
  → `packages/shared/dist/` 產生 cjs 與 esm 兩份輸出。
- [ ] `a7` **［終端機］** 建立資料庫 schema。
  → **依 `DATABASE_URL` 建出中間缺的資料夾與 SQLite 檔案**，套用五個 migration（`init`、`iteration3_closed_network`、`iteration4_ai_content`、`iteration5_help_resolution_shifts_baseline`、`iteration6_carousel_file_import`），最後顯示資料庫已同步。資料庫是空的時候 Prisma 會順便跑一次 seed，下一步再跑也不會重複建立。
- [ ] `a8` **［終端機］** 產生合成測試資料。
  → 建立 **8 位病人**（`HD-TEST-0001`～`0008`）與 **15 台平板**（`TB-01`～`TB-15`），裝置金鑰寫入 `apps/api/.device-keys.json`。**這 15 台平板預設尚未納入 MDM**，稍後要手動註冊。
- [ ] `a13` **［終端機］** 確認資料庫檔案真的生出來了。
  → 你在 `a11` 指定的資料夾已經存在，裡面有 `hd.db`（seed 完約 700 KB 起跳）。看到它就代表「填路徑 → 檔案誕生」這件事完成了，可以往下啟動。後端跑起來之後（`a9`）旁邊會再多出 `hd.db-wal` 與 `hd.db-shm` 兩個 WAL 附屬檔，**要搬移或備份時三個是一組**，不要只拿主檔。

```bash
npm run build:shared
npm run prisma:migrate
npm run db:seed
```

*`a13`：把路徑換成你在 `a11` 填的*

```bash
ls -l /d/hd-data            # Git Bash
# PowerShell：Get-ChildItem D:\hd-data
```

這三行各做一件事，缺一不可：

| 指令 | 在做什麼 | 為什麼需要 |
|---|---|---|
| `npm run build:shared` | 把 `packages/shared` 的 TypeScript 編譯成 JS。裡面是**前後端共用的型別與常數**：問卷題庫、求助分派規則、68 種稽核動作代碼、權限碼。 | 後端與兩個 PWA 都會 import 它。沒編譯過，後端一啟動就找不到模組。**之後每次改動 shared 都要重跑**（`npm run dev` 會自動先跑一次）。 |
| `npm run prisma:migrate` | 讀 `apps/api/prisma/schema.prisma`，把資料表建到 `.env` 的 `DATABASE_URL` 指向的 SQLite 檔案，並產生 Prisma Client。本專案目前有五個 migration（迭代 3～6 各一個）。 | 你的資料庫一開始是**空的**。這步建出 nurses、patients、devices、device_bindings、symptom_reports、help_requests、audit_logs 等資料表。沒跑過，後續任何操作都會失敗。 |
| `npm run db:seed` | 灌入**虛構**測試資料：8 位病人（`HD-TEST-0001`～`0008`）與 15 台平板（`TB-01`～`TB-15`），並把平板金鑰寫到 `apps/api/.device-keys.json`。 | 沒有病人就不能建排班，沒有平板就不能指派。**它不建立任何護理師帳號** — 那是後端啟動時依 `.env` 自動建立的。可重複執行，已有資料時會自動略過。 |

> **地雷｜`.device-keys.json` 只對應「最後一次 seed 的那顆資料庫」**
>
> `db:seed` 寫出的 `apps/api/.device-keys.json` 是**明文金鑰**，而資料庫裡只存雜湊值。
> 拿另一個 `DATABASE_URL` 再跑一次 `prisma:migrate` 或 `db:seed`（例如 §12 換資料庫、或臨時試另一個檔案），
> 這個檔案會被**整份覆蓋**，原本那顆資料庫的平板金鑰就再也拿不回來，病人端只會停在「裝置尚未佈建」。
> 補救只有兩條：在護理端「裝置管理」重新註冊幾台新平板（`f20`，會當場給新金鑰），或照 §11 重置資料庫重來。

### 啟動

- [ ] `a9` **［終端機］** 一次啟動後端與兩個 PWA。
  → 三色標示的 log 同時輸出。後端出現「後端服務已啟動：http://localhost:3000/api」。
- [ ] `a10` **［終端機］** 在後端 log 中找「已建立初始最高權限帳號」。
  → 全新資料庫會看到這行，代表 `.env` 的帳密已建立成 SUPER_ADMIN。若資料庫已有使用者則不會出現（正常）。
  看到的若是黃字「資料庫中尚無任何使用者，但 .env 未設定 SUPER_ADMIN_WORK_ID / SUPER_ADMIN_INITIAL_PASSWORD⋯⋯」，代表 `a5` 那兩個變數漏填了 — **後端會照常啟動，但沒有任何帳號可以登入**。補進 `.env` 後重新啟動即可，資料庫不必重建。

```bash
npm run dev
```

> **補充｜只想跑其中一個**
>
> `npm run dev:api` / `npm run dev:nurse` / `npm run dev:patient` 可分別啟動。改動 `packages/shared` 後要重跑 `npm run build:shared` 才會生效。

## 02 · 冒煙測試

花兩分鐘確認三個服務都活著，再往下走。

- [ ] `b1` **［護理端］** 開啟 `http://localhost:5173`。
  → 看到「護理站主控台」登入卡片，有「登入 / 註冊申請」兩個分頁。
- [ ] `b2` **［病人端］** 開啟 `http://localhost:5174`（不帶任何參數）。
  → 看到「**裝置尚未佈建**」畫面。這是給院方人員設定平板用的，不是病人登入 — 正式環境由 MDM 直接把序號與金鑰寫進 Kiosk 網址，病人永遠看不到這頁。
- [ ] `b3` **［終端機］** 確認 API 有回應。
  → 回傳 `401`。沒帶權杖被拒是**正確**的 — 代表路由有掛上、驗證有生效。

*Git Bash / cmd.exe*

```bash
curl -i http://localhost:3000/api/overview
```

*PowerShell — 注意是 curl.exe*

```bash
curl.exe -i http://localhost:3000/api/overview
```

> **注意｜PowerShell 要打 curl.exe**
>
> PowerShell 把 `curl` 設成 `Invoke-WebRequest` 的別名，它**不認得 `-i`、`-s`、`-H` 這些參數**，會噴「找不到符合參數名稱 i 的參數」。加上 `.exe` 才會呼叫到真正的 curl。**本手冊後面所有 curl 指令，在 PowerShell 都要改成 `curl.exe`**；Git Bash 與 cmd.exe 直接用 `curl` 即可。

## 03 · 主線 A — 裝置綁定完整流程

這是 Iteration 1 的驗收主線：**登入 → 核准護理師 → 建排班 → 指派平板 → 病人端顯示 → 解除綁定 → 稽核可查**。走完這一段，系統的骨架就確認可用了。

### 身份驗證與審核

- [ ] `c1` **［護理端］** 以 `.env` 的 `SUPER_ADMIN_WORK_ID` 與初始密碼登入。
  → 登入失敗跳「工作ID 或密碼錯誤」請看下方的排查框。**全新資料庫**：直接被導到「變更密碼」畫面，藍色提示說明此帳號正在使用 .env 的初始密碼，未變更前不能使用其他功能。
- [ ] `c2` **［護理端］** 設定新密碼（至少 8 字元，兩次輸入需一致）。
  → 顯示「密碼已變更 / 所有既有登入階段已撤銷」，必須用**新密碼**重新登入。**請記下新密碼** — .env 裡的舊密碼從此失效。
- [ ] `c3` **［護理端］** 重新登入，確認進入主控台。
  → 頂端導覽列出現：即時總覽、緊急通報、排班與指派、帳號審核、病人、裝置管理、稽核軌跡。右上顯示你的工作ID 與 `SUPER_ADMIN`。
- [ ] `c4` **［護理端］** 登出 → 切到「註冊申請」分頁 → 申請一個新帳號，工作ID 用 `N001`、姓名自訂、密碼至少 8 字元。
  → 顯示申請已送出、等待審核。
- [ ] `c5` **［護理端］** 立刻用 `N001` 嘗試登入。
  → 被拒，訊息「註冊申請尚待審核，核准後才能登入」（HTTP 403）。這是 3.1 節第 2 點的要求。
- [ ] `c6` **［護理端］** 以管理者登入 → 「帳號審核」→ 在「待審核註冊申請」找到 `N001` → 按「核准」。
  → 綠色提示「已核准 N001，該帳號現在可以登入」，下方「帳號與權限」表中該列狀態變成 `ACTIVE`。
- [ ] `c7` **［護理端］** 登出，改用 `N001` 登入。
  → 登入成功。導覽列**只有**即時總覽、緊急通報、排班與指派、病人、裝置管理 — 沒有「帳號審核」與「稽核軌跡」（一般護理師無此權限）。

> **地雷｜登入一直顯示「工作ID 或密碼錯誤」**
>
> 這個訊息只代表兩件事之一：**工作ID 不存在**，或**密碼不對**（刻意不區分，避免洩漏帳號是否存在）。依序排查：
> **① 別用打的，用複製貼上。**最常見的兇手是**中文輸入法**：密碼結尾若有 `!`，中文模式下會打成全形的 `！`，長度一樣但完全不同的字元。切到英數模式，或直接從 `.env` 複製貼上。
> **② 檢查瀏覽器自動填入**有沒有蓋掉你輸入的值（欄位變黃底就是）。
> **③ 你是不是已經改過密碼了？**手冊的 `c2` 步驟會強制變更密碼，**改完之後 .env 裡的初始密碼就永久失效**。
> **④ 用下面的指令直接驗**（完全不經過鍵盤與瀏覽器）。

*在專案根目錄執行，用 .env 的原值直接打 API*

```bash
node -r dotenv/config -e "fetch('http://localhost:3000/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({workId:process.env.SUPER_ADMIN_WORK_ID,password:process.env.SUPER_ADMIN_INITIAL_PASSWORD})}).then(r=>Promise.all([r.status,r.text()])).then(([s,t])=>console.log(s,t.slice(0,80)))" dotenv_config_path=.env
```

> **補充｜怎麼判讀上面的結果**
>
> 印出 **200** 加一串 `accessToken` → 帳號與密碼**都是對的**，問題百分之百出在瀏覽器的輸入（回頭看 ① 和 ②）。
> 印出 **401** → 資料庫裡的密碼確實跟 `.env` 不一樣了，多半是已經改過密碼。用你當初設定的**新密碼**登入；真的想不起來，就照 §10 重置資料庫。
> 印出 **403** → 帳號存在但狀態不是 ACTIVE。
> 連不上（ECONNREFUSED）→ 後端沒啟動。

### 建立病人、平板與排班

- [ ] `c8` **［護理端］** 以**管理者**登入 →「病人」→ 建立一位病人：病歷號 `HD-TEST-0009`、姓名「王測試」。
  → 出現在病人清單中。頁面上方常駐一則藍色提示，重申測試資料必須虛構。
- [ ] `c9` **［護理端］** 「裝置管理」→ 在平板清單找到 `TB-01` → 按「MDM 註冊」。
  → MDM 欄由「未納管」變成「**已納管**」。所有 MDM 動作都經後端 `MdmProviderPort`，本階段是 MockMdmProvider。
- [ ] `c10` **［護理端］** 「排班與指派」→ 日期選今天 → 病人選「王測試」→ 時段選「早班」→ 按「建立排班」。
  → 表格新增一列，狀態 `SCHEDULED`，綁定平板欄為「—」。
- [ ] `c11` **［護理端］** 在該列右側的下拉選單選 `TB-01` → 按「指派平板」。
  → 狀態變 `IN_PROGRESS`，綁定平板欄顯示 `TB-01`，「有效至」顯示**六小時後**的時間（`BINDING_MAX_HOURS` 預設值）。

> **補充｜為什麼下拉選單裡看不到某些平板**
>
> 可指派的平板必須**同時**符合：已完成 MDM 納管、未被遠端鎖定、目前沒有綁定其他病人。三者缺一就不會出現在清單裡。

### 病人端平板

- [ ] `c12` **［終端機］** 開啟 `apps/api/.device-keys.json`，找出 `TB-01` 的 `apiKey`。
  → 一個長度 43 的 base64url 字串。這個檔案是 seed 產生的明文金鑰，已列入 .gitignore。
- [ ] `c13` **［病人端］** 用下面的網址開啟病人端（把金鑰換成你的）。
  → 約 3 秒後畫面由「請由護理人員為您開始使用」切換為**已綁定畫面**：病人姓名、病歷號、療程時段（早班）、開始時間、本次使用期限。網址列的金鑰會**自動被清掉**，避免病人看到。
- [ ] `c14` **［病人端］** 觀察畫面右下角。
  → 顯示平板序號 `TB-01`。整個畫面**沒有任何登入欄位** — 這是 FR-P01 的硬性要求。

*病人端佈建網址*

```bash
http://localhost:5174/?serial=TB-01&key=<貼上金鑰>
```

### 解除綁定與稽核

- [ ] `c15` **［護理端］** 回到「排班與指派」，在該列按「**手動解除**」。
  → 綁定平板欄回到「—」，排班狀態**回到 `SCHEDULED`**（可再次指派，適用於換平板的情境）。
- [ ] `c16` **［病人端］** 觀察病人端（不要重新整理）。
  → 3 秒內自動回到等待畫面，並出現一行綠字「**前次療程的本地資料已清除**」。這是 SRS 4.3 步驟 8。
- [ ] `c17` **［護理端］** 再指派一次 `TB-01`，這次按「**下機結束**」。
  → 排班狀態變 `COMPLETED`，該列不再顯示操作按鈕。兩種解除方式對排班狀態的影響不同，這是刻意設計。
- [ ] `c18` **［護理端］** 「稽核軌跡」→ 逐一用「全部動作」下拉篩選，確認以下事件都查得到。
  → 登入成功、護理師註冊申請、核准護理師註冊、建立病人資料、MDM 裝置註冊、建立當日排班、裝置綁定、Session Token 送達平板、解除綁定、平板本地暫存資料已清除。
- [ ] `c19` **［護理端］** 找到「裝置綁定」那一列，檢查欄位。
  → 同時包含**操作護理師帳號**、**時間戳**、**病人**、**平板序號**與說明文字 — SRS 4.3 步驟 9 明列的四項必要稽核欄位。

## 04 · 主線 B — 症狀回報與即時運作

Iteration 2 的驗收主線：**病人填問卷 → 護理端即時更新 → 病人求助 → 護理端收到通報與趨勢摘要 → 接手處理**。

> **注意｜前置**
>
> 需要一組**進行中的綁定**。若剛才已按過「下機結束」，請重新建立一筆排班並指派平板（重複 `c10`–`c13`）。建議把護理端與病人端**並排開在兩個視窗**，才看得到即時效果。

### FR-P02 透析前症狀問卷

- [ ] `d1` **［病人端］** 在已綁定畫面點「填寫透析前症狀問卷」。
  → 進入問卷頁，共 **10 題**，每題預設都停在「沒有」，右上顯示「已勾選 0 項」。
- [ ] `d2` **［病人端］** 確認題目與作答方式。
  → 8 題是四級量表（沒有／輕微／中等／嚴重），2 題是二選一（有／沒有）。見下表。

| 題目 | 說明文字 | 作答方式 |
|---|---|---|
| 身體水腫 | 腳踝、小腿或臉部比平常腫 | 四級 |
| 呼吸會喘 | 走路、平躺或休息時覺得喘 | 四級 |
| 發燒或畏寒 | 今天有發燒、忽冷忽熱 | **有／沒有** |
| 血管通路出血 | 上次拔針的地方滲血或流血不止 | **有／沒有** |
| 血管通路紅腫或疼痛 | 瘻管、導管周圍紅、腫、熱、痛 | 四級 |
| 胸悶或胸痛 | 胸口悶、壓迫感或疼痛 | 四級 |
| 頭暈 | 站起來或平常時候會頭暈 | 四級 |
| 抽筋 | 上次透析後到現在有抽筋 | 四級 |
| 噁心想吐 | 覺得反胃、想吐 | 四級 |
| 皮膚搔癢 | 皮膚癢到影響睡眠或抓破皮 | 四級 |

- [ ] `d3` **［病人端］** 把「身體水腫」點成**中等**、「發燒或畏寒」點成**有**。
  → 右上計數變成「已勾選 **2** 項」。四題以外的其他題目維持「沒有」。
- [ ] `d4` **［病人端］** 最下方的自由文字欄輸入一句話（選填），然後按「送出給護理站」。
  → 回到主畫面，出現綠字「**已送出給護理站，謝謝您。**」，問卷按鈕副標變成「今天已送出 1 次」。

### FR-N02 多病人即時狀態總覽

- [ ] `d5` **［護理端］** 切到「即時總覽」，看右上角的連線狀態。
  → 綠色標籤「**即時連線中**」。這代表 SSE 串流已建立（護理端用 fetch 串流而非 EventSource，才能帶 Authorization 標頭）。
- [ ] `d6` **［護理端］** 找到該病人那一列，看「病人自述症狀」欄。
  → 出現兩個藍色標籤「**身體水腫・中等**」「**發燒或畏寒・有**」，**不需要手動重新整理**。
- [ ] `d7` **［護理端］** 檢查同一列的其他欄位。
  → 平板欄顯示序號＋綠點「連線中」；療程進度顯示進度條與「X小時Y分」；最後活動顯示剛才送出的時間。
- [ ] `d8` **［護理端］** 點該列最右側的「看問卷」。
  → 展開答案格，10 題全部列出；病人自述**有**症狀的兩題以**紅底**標示。若有填自由文字，下方顯示「病人補充：」。
- [ ] `d9` **［護理端］** 看表格最下方的說明文字。
  → 「以上為病人自行填寫之症狀原樣彙整，僅供決策輔助，非風險評估或診斷結果。」— SRS 第 8 章的要求。系統**不做**任何風險分層。

### FR-P06 求助按鈕

- [ ] `d10` **［病人端］** 回主畫面 → 點紅框的「我需要協助」。
  → 進入求助頁：上半是 7 種求助類型，下半是 3 種急迫度。
- [ ] `d11` **［病人端］** **只選類型不選急迫度**，觀察底部按鈕。
  → 「通知護理站」是**停用**狀態。兩項都選完才會啟用 — 避免不完整的通報。
- [ ] `d12` **［病人端］** 選「**打針處或管路有狀況**」＋「**非常緊急，請馬上過來**」，補充說明可填可不填，按「通知護理站」。
  → 回主畫面，顯示「已通知護理站，護理人員將盡快過來。」底部出現「您的求助：待處理」。

### FR-N03 緊急通報接收

- [ ] `d13` **［護理端］** 留在**任何一頁**觀察（不必切到緊急通報頁）。
  → 頁面頂端跳出**紅色橫幅**：病人姓名與病歷號、求助類型、急迫度、平板序號，右側有「前往處理 / 稍後」。導覽列的「緊急通報」出現紅色數字徽章 **1**。
- [ ] `d14` **［護理端］** 回到「即時總覽」。
  → 「待處理求助」與「其中標示非常緊急」兩張統計卡**變成紅底**並顯示 1；該病人那列被移到**最上方**並以紅底highlight，求助欄出現紅色標籤。
- [ ] `d15` **［護理端］** 按橫幅上的「前往處理」。
  → 跳到「緊急通報」頁，卡片為紅底，右上三個標籤：急迫度、「**分派：緊急處置團隊**」、「待處理」。
- [ ] `d16` **［護理端］** 確認卡片中間那行摘要。
  → 顯示「本次療程問卷（時間）：身體水腫・中等、發燒或畏寒・有」— 護理師到場前就掌握背景，這正是 FR-P06 的重點。
- [ ] `d17` **［護理端］** 點「查看近期症狀趨勢摘要」。
  → 展開表格：症狀 / 自述次數（如 `1 / 1`）/ 最近一次 / 時間，下方再次出現非診斷結果的提示。預設統計近 30 天、最多 10 筆回報。
- [ ] `d18` **［護理端］** 按「**我接手**」。
  → 狀態標籤變「已接收，前往中」，「我接手」按鈕消失，只剩「處理完成 / 取消」。
- [ ] `d19` **［病人端］** 等約 10 秒觀察病人端底部。
  → 「您的求助」由「待處理」變成「**已接收，前往中**」。病人知道有人要來了。
- [ ] `d20` **［護理端］** 按「處理完成」。
  → 該卡片從待處理清單消失，導覽列徽章歸零，總覽的兩張紅色統計卡回到 0 並恢復灰底。

## 05 · 離線韌性

SRS 4.4 對離線行為有兩條**方向相反**的要求：症狀回報必須能暫存並自動補傳；求助則必須明確告知無法通知護理站。這一節同時驗證兩者。

> **注意｜為什麼求助不做離線暫存**
>
> 讓病人**誤以為**護理站已收到，比誠實說明送不出去更危險。因此離線時求助按鈕直接停用並引導改用床邊呼叫鈴。這是刻意的設計取捨，不是缺漏。

- [ ] `e1` **［病人端］** 在病人端按 F12 開 DevTools → Network 分頁 → 把 Throttling 下拉選成「**Offline**」。
  → 只影響這個分頁，護理端不受影響。
- [ ] `e2` **［病人端］** 等 3–5 秒觀察主畫面。
  → 出現黃字「目前沒有網路連線。症狀問卷仍可填寫並暫存於本平板；求助請改用床邊呼叫鈴。」求助按鈕副標變成「**目前無法即時通知護理站**」。
- [ ] `e3` **［病人端］** 點進「我需要協助」。
  → 頂端橘色警示框：「目前無法即時通知護理站。請改用床邊呼叫鈴，或直接出聲呼叫附近的護理人員。」底部按鈕顯示「**離線中，無法通知**」且**停用**。
- [ ] `e4` **［病人端］** 回上一頁 → 點進「填寫透析前症狀問卷」。
  → 頂端黃字：「目前沒有網路連線。您仍可以填寫，送出後會暫存在這台平板，恢復連線時自動傳送給護理站。」**問卷本身完全可用**（題庫打包在前端，不需連線）。
- [ ] `e5` **［病人端］** 把「呼吸會喘」點成**嚴重**，按「送出給護理站」。
  → 回主畫面顯示綠字「**已暫存在這台平板。恢復連線後會自動送給護理站，資料不會遺失。**」以及「尚有 **1** 筆症狀回報等待傳送」。
- [ ] `e6` **［病人端］** 把 Throttling 改回「No throttling」，等約 10 秒。
  → 離線提示消失，出現綠字「**已將 1 筆離線暫存的症狀回報送出給護理站**」，等待傳送的計數歸零。
- [ ] `e7` **［護理端］** 看即時總覽該病人列。
  → 病人自述症狀更新為「**呼吸會喘・嚴重**」（顯示的是最新一份回報）。
- [ ] `e8` **［護理端］** 「稽核軌跡」→ 篩選「病人送出症狀問卷」→ 看最新一列的說明欄。
  → 包含「送出方式 **離線暫存後補傳**」。系統把即時送出與離線補傳分開記錄，且**保留病人當初填寫的時間**，不會被補傳時間覆寫。

> **說明｜資料為什麼不會掉**
>
> 病人按下送出時，資料是**先寫進 IndexedDB 佇列、再嘗試上傳**，成功才移除。就算平板在上傳途中斷電或被 Kiosk 重新載入，資料仍在。後端再用平板產生的識別碼去重，所以重複補傳也不會變成兩筆。

## 06 · 細節功能逐項

主線走通之後，這一節把每個分支、每個錯誤路徑都掃過一遍。可以分次做完。

### 6.1 身份驗證與帳號

- [ ] `f1` **［護理端］** 用**已存在**的工作ID 再次申請註冊。
  → 409「此工作ID 已被註冊或已提出申請」。
- [ ] `f2` **［護理端］** 註冊時密碼只輸入 5 個字。
  → 400「密碼長度至少 8 字元」。
- [ ] `f3` **［護理端］** 註冊時工作ID 輸入中文（例如「護理師01」）。
  → 400「工作ID 僅能包含英數字、底線與連字號，長度 3–32 字元」。
- [ ] `f4` **［護理端］** 用正確帳號但**錯誤密碼**登入。
  → 401「工作ID 或密碼錯誤」。
- [ ] `f5` **［護理端］** 用**不存在**的工作ID 登入。
  → **完全相同**的 401 訊息。刻意不透露帳號是否存在；後端連回應時間都對齊了（不存在時仍比對一次假雜湊）。
- [ ] `f6` **［護理端］** 以管理者查「稽核軌跡」→ 篩選「登入失敗」。
  → 剛才兩次失敗都有記錄，整列以**紅底**顯示（outcome = FAILURE）。密碼錯誤與帳號不存在的說明文字不同 — 內部可追查，對外不洩漏。
- [ ] `f7` **［護理端］** 再申請一個帳號 `N002`，然後在「帳號審核」按「**駁回**」，於彈出視窗輸入原因。
  → 狀態變 `REJECTED`；該帳號登入被拒（403「此帳號目前無法登入，請洽護理站管理者」）。稽核記錄含駁回原因。
- [ ] `f8` **［護理端］** 右上「變更密碼」→ 目前密碼與新密碼**填一樣**。
  → 400「新密碼不可與目前密碼相同」。
- [ ] `f9` **［護理端］** 正常變更一次密碼。
  → 完成後顯示所有既有登入階段已撤銷，必須重新登入。這是自建 Session 才做得到的主動撤銷。
- [ ] `f10` **［護理端］** 按「登出」。
  → 回到登入頁；原權杖立刻失效（可用 §08 的 curl 驗證：登出後再帶舊 token 呼叫 `/auth/me` 會得到 401）。

### 6.2 權限矩陣（SRS 4.2）

| 權限 | 一般護理師 | 護理長 | 護理站管理者 | 最高權限 |
|---|---|---|---|---|
| 建立排班、指派／解除裝置 | ✓ | ✓ | ✓ | ✓ |
| 檢視症狀回報、多病人即時總覽 | ✓ | ✓ | ✓ | ✓ |
| 處理緊急通報（接手／到床邊／結案） | ✓ | ✓ | ✓ | ✓ |
| 衛教內容管理、護理記錄簽核、SOP 查詢 | ✓ | ✓ | ✓ | ✓ |
| 輪播第二層內容上下架 | ✓ | ✓ | ✓ | ✓ |
| 審核護理師註冊 | 經授予後可 | 經授予後可 | ✓ | ✓ |
| 病人資料維護 | — | — | ✓ | ✓ |
| MDM 裝置管理 | — | — | ✓ | ✓ |
| 稽核軌跡查詢 | — | — | ✓ | ✓ |
| 功能開關（高風險、輪播、績效） | — | ✓ | ✓ | ✓ |
| 臨床數值維護 | — | ✓ | ✓ | ✓ |
| 班表與床位分配、成效基準、營運參數與選項清單 | — | ✓ | ✓ | ✓ |
| 資料庫備份與還原 | — | — | ✓ | ✓ |
| AI 相關開關與模型供應者設定 | — | — | — | ✓ |
| 指派他人管理權限 | — | — | — | ✓ |

另有兩個唯讀角色：**醫院管理層（唯讀）**在 L1 聚合儀表板（迭代 8）之前沒有任何權限，**稽核／法規（唯讀）**只讀得到稽核軌跡與 AI 呼叫紀錄。兩者的測試項在[迭代 3 分冊](iteration-3.md) `l11`–`l14`；後端對唯讀角色是**預設全擋**，只放行明確標示開放的端點。

- [ ] `f11` **［護理端］** 以一般護理師 `N001` 登入，檢查導覽列。
  → 看不到「帳號審核」與「稽核軌跡」。
- [ ] `f12` **［護理端］** 以 `N001` 進入「病人」頁。
  → 只有清單，**沒有**建立表單（表單依權限隱藏）。
- [ ] `f13` **［護理端］** 以 `N001` 進入「裝置管理」，試著按任一台的 MDM 按鈕。
  → 清單看得到（指派平板需要），但操作會跳出紅色橫幅「權限不足，缺少：device:manage」。
- [ ] `f14` **［護理端］** 以管理者在「帳號審核」→「帳號與權限」表對 `N001` 按「**授予審核權限**」。
  → 審核權限欄由「無」變「有」。`N001` 重新登入後導覽列出現「帳號審核」— 這是 3.1 節第 4 點的權限分享。
- [ ] `f15` **［護理端］** 在 `N001` 那一列右側的**角色下拉選單**選「護理站管理者」。
  → 選單一改就立即送出（沒有另外的儲存鈕），提示「`N001` 的角色已改為「護理站管理者」」；角色欄變 `NURSE_MANAGER`，重新登入後可看到「稽核軌跡」、病人建立表單、MDM 操作可用。
- [ ] `f16` **［護理端］** 讓 `N001` 保持登入，然後以管理者把同一個下拉選單改回「一般護理師」。
  → `N001` 的下一次操作會得到 401 並被踢回登入頁 — **權限縮減會一併撤銷該帳號既有登入階段**，舊權杖不能沿用舊權限。
- [ ] `f64` **［護理端］** 拉開那個角色下拉選單，數一下有幾個選項，然後把 `N001` 改成「**護理長／單位主管**」。
  → 五個可指派的角色：護理站管理者、護理長／單位主管、一般護理師、醫院管理層（唯讀）、稽核／法規（唯讀）（最高權限不在選單裡，不能指派）。改成護理長後重新登入，導覽列出現「班表」「成效基準」「臨床數值」與「系統管理」，但**系統管理裡沒有「資料庫與備份」，AI 那兩個開關標「僅限系統管理者」**。
  **迭代 3、5、6 三本分冊的前置都要一個護理長帳號，就是在這裡生出來的** — 建議另外申請一個 `N003` 專門當護理長，`N001` 留著當一般護理師。
- [ ] `f17` **［護理端］** 在「帳號與權限」表找自己那一列，以及 SUPER_ADMIN 那一列。
  → 兩者的權限指派欄都顯示「—」：不可變更自己的權限，也不可變更最高權限帳號。

### 6.3 病人資料

- [ ] `f18` **［護理端］** 建立病人時病歷號輸入 `ab`（少於 4 字元）。
  → 400「病歷號格式不正確」。合法格式為英數字與連字號、4–32 字元。
- [ ] `f19` **［護理端］** 用**已存在**的病歷號再建立一次。
  → **500 Internal server error**。

> **注意｜已知粗糙處**
>
> 重複病歷號目前沒有被特別處理，資料庫的唯一鍵衝突直接冒成 500，而不是像其他地方一樣回 409 加中文訊息。**資料本身沒有寫壞**（唯一鍵擋住了），只是錯誤訊息不友善。已記錄，待後續迭代修。

### 6.4 裝置與 MDM（FR-S01）

- [ ] `f20` **［護理端］** 「裝置管理」→ 註冊新平板，序號 `TB-99`、標籤自訂。
  → 一次完成三件事：建立裝置、**自動 MDM 納管**、設定 Kiosk 網址。畫面出現藍色框，內含**一次性的佈建網址**（含序號與金鑰）。
- [ ] `f21` **［護理端］** 重新整理頁面，想再看一次剛才的金鑰。
  → 看不到了。**金鑰只顯示一次**，資料庫只存雜湊。若沒複製到，只能重新註冊一台。
- [ ] `f22` **［護理端］** 用相同序號 `TB-99` 再註冊一次。
  → 409「此平板序號已註冊」。
- [ ] `f23` **［護理端］** 對一台**未綁定**的已納管平板按「遠端鎖定」。
  → MDM 欄變成「已納管・**鎖定中**」。
- [ ] `f24` **［護理端］** 回「排班與指派」，看該平板是否還在可指派下拉選單中。
  → **不在**。鎖定中的平板不可指派。
- [ ] `f25` **［護理端］** 按「解除鎖定」，再回去看下拉選單。
  → 重新出現，可以指派。
- [ ] `f26` **［護理端］** 對某台平板按「解除註冊」。
  → MDM 欄變「未納管」，按鈕變回「MDM 註冊」；該平板同樣不可指派（400「此平板尚未納入 MDM 管理，不可指派給病人」）。
- [ ] `f27` **［護理端］** 檢查稽核軌跡的 MDM 事件。
  → 五種動作都有獨立記錄：MDM 裝置註冊、MDM 解除註冊、MDM 遠端鎖定、MDM 解除鎖定、MDM Kiosk 模式設定，說明欄前綴 `[mock]` 標示目前的 provider。

### 6.5 排班（FR-N01）

- [ ] `f28` **［護理端］** 為**同一位病人、同一天、同一時段**重複建立排班。
  → 409「該病人在此日期時段已有排班」。（病人＋日期＋時段是資料庫唯一鍵）
- [ ] `f29` **［護理端］** 同一位病人改成**不同時段**（早班→午班）建立。
  → 成功。同一天可以有多個時段。
- [ ] `f30` **［護理端］** 把日期選成昨天或明天。
  → 顯示該日期的排班（可能是空的）。日期切換只影響「排班與指派」頁。

> **補充｜只有 API 有、UI 尚未提供的功能**
>
> 「**取消排班**」（`POST /api/treatment-sessions/:id/cancel`）目前只有後端端點，護理端畫面上還沒有按鈕。要測它請用 §08 的 curl。規則是：該排班若仍有進行中的綁定，取消會被擋（409）。

### 6.6 裝置綁定例外情況（SRS 4.4）

- [ ] `f31` **［護理端］** 同一位病人建立兩筆排班（早班、午班），先指派 `TB-01` 給早班，再試著指派 `TB-02` 給午班。
  → 紅色橫幅「該病人已有進行中之綁定（平板 TB-01），請先解除後再指派」（409）。這是 SRS 4.4 明列的重複綁定阻擋。
- [ ] `f32` **［護理端］** 把另一位病人的排班指派給**已被佔用**的 `TB-01`。
  → 409「平板 TB-01 目前已指派給其他病人，請先解除後再指派」。（實務上下拉選單已濾掉，用 curl 可直接打到這條）
- [ ] `f33` **［護理端］** 「稽核軌跡」→ 篩選「綁定遭阻擋：病人已有進行中之綁定」。
  → **被擋下的嘗試本身也留下紅底記錄**，含操作者、病人與平板序號。阻擋不是靜默失敗。
- [ ] `f34` **［護理端］** 對已「下機結束」（`COMPLETED`）的排班嘗試指派平板。
  → 400「此排班狀態為 COMPLETED，無法指派平板」。

### 6.7 綁定逾時自動失效

第三種失效條件是「超過療程時間上限」，預設 6 小時。不必真的等六小時 — 直接把到期時間改到過去即可。

- [ ] `f35` **［終端機］** 先建立一組進行中的綁定並開著病人端，然後另開一個終端機啟動 Prisma Studio。
  → 瀏覽器開啟 `http://localhost:5555`。
- [ ] `f36` **［資料庫］** 在 Prisma Studio 開啟 `device_bindings` 資料表 → 找到 `status = ACTIVE` 的那一列 → 把 `expires_at` 改成**過去**的時間 → Save。
  → 存檔成功。
- [ ] `f37` **［病人端］** 回到病人端等 3 秒（下一次輪詢）。
  → 立刻回到等待畫面並顯示資料已清除。平板每次查詢綁定狀態時也會自行檢查到期，不必等背景掃描。
- [ ] `f38` **［護理端］** 「稽核軌跡」→ 篩選「綁定逾時自動失效」。
  → 有一列記錄，**操作者是 `SYSTEM`**（不是護理師），說明欄註明超過療程時間上限。排班狀態變 `COMPLETED`。

*啟動 Prisma Studio*

```bash
cd apps/api
npm run prisma:studio
```

> **補充｜另一條路徑**
>
> 背景排程每 `BINDING_EXPIRY_SWEEP_SECONDS` 秒（預設 60）掃一次逾期綁定。如果平板沒有開著，最多一分鐘後也會自動失效。

### 6.8 病人端 Session 隔離（SRS 4.3 步驟 5–8）

- [ ] `f39` **［病人端］** 在**未綁定**狀態開啟病人端，用 DevTools → Network 看 `/api/device/session` 的回應。
  → `{"state":"WAITING", ...}` — **完全不含**任何病人姓名或病歷號。等待畫面不可洩漏前一位病人的資訊。
- [ ] `f40` **［病人端］** 把網址的 `key` 改成隨便一串字重新開啟。
  → 回到「裝置尚未佈建」畫面並顯示「裝置序號或金鑰不正確」（401）。
- [ ] `f41` **［終端機］** 用 curl 拿 A 平板的 Session Token，改用 B 平板的序號與金鑰去呼叫 `/api/device/session/context`。
  → 401「Session Token 與本裝置不符」。Token 綁死在核發的那台平板上。
- [ ] `f42` **［終端機］** 解除綁定後，再用剛才那個 Session Token 呼叫一次。
  → 401「綁定已失效，請洽護理人員重新指派」。

### 6.9 症狀問卷細節

- [ ] `f43` **［病人端］** 全部題目都留在「沒有」，直接送出。
  → 送出成功。護理端該列顯示「**已填寫，皆勾選『沒有』**」（與「尚未填寫」是不同狀態）。
- [ ] `f44` **［病人端］** 在同一次療程中連續送出**三份**問卷。
  → 都成功。護理端總覽顯示**最新一份**的症狀；病人端按鈕副標累計「今天已送出 3 次」。
- [ ] `f45` **［護理端］** 展開「看問卷」，確認顯示的是哪一份。
  → 最新一份，標題後方註明填寫時間；若該份是離線補傳，會多顯示「（離線暫存後補傳）」。
- [ ] `f46` **［護理端］** 在緊急通報頁展開趨勢摘要，看「自述次數」欄。
  → 分母是納入統計的回報筆數。送出三份後，某個症狀可能顯示 `2 / 3` — 純粹是次數統計，**不加權、不分級、不預測**。
- [ ] `f47` **［病人端］** 解除綁定後，重新指派給**另一位病人**，開啟病人端。
  → 看不到前一位病人的任何回報。平板只能存取本次綁定的資料（SRS 4.3 步驟 6）。

### 6.10 求助分派規則（FR-N03）

分派只看**病人自選**的類型與急迫度，不讀歷史資料、不做推論 — 所以不落入已排除的風險分層範疇。規則表如下，可逐格驗證：

| 急迫度 ＼ 類型 | 臨床類（身體不舒服／打針處或管路有狀況／機器在叫） | 非臨床類（需要上廁所／環境調整／口渴或肚子餓／其他） |
|---|---|---|
| **非常緊急**，請馬上過來 | 緊急處置團隊 | 緊急處置團隊 |
| **不太舒服**，請盡快過來 | 護理師 | 護理師 |
| **不急**，方便時再過來 | 護理師 | **護佐／行政支援** |

- [ ] `f48` **［病人端］** 送出「需要上廁所」＋「不急，方便時再過來」。
  → 護理端卡片顯示「分派：**護佐／行政支援**」— 降低對護理師的無效打斷。
- [ ] `f49` **［病人端］** 送出「需要上廁所」＋「非常緊急，請馬上過來」。
  → 「分派：**緊急處置團隊**」。急迫度優先於類型。
- [ ] `f50` **［病人端］** 送出「機器在叫」＋「不急，方便時再過來」。
  → 「分派：**護理師**」。臨床類型即使不急也不會下放給護佐。
- [ ] `f51` **［護理端］** 對一筆通報按「**取消**」而非「處理完成」。
  → 從待處理清單消失，稽核記錄動作為「緊急通報取消」。
- [ ] `f52` **［護理端］** 病人送出一筆求助後**不處理**，直接對該綁定按「下機結束」。
  → 該筆通報自動收斂，不再留在護理端清單。稽核出現一列由 `SYSTEM` 記錄的「緊急通報取消」，說明「綁定已失效（正常下機結束），未處理完成之通報一併取消」。
- [ ] `f53` **［病人端］** 在求助頁快速**連按兩下**送出鈕。
  → 護理端只出現**一筆**通報。前端以識別碼去重，後端也有唯一鍵把關。

### 6.11 即時總覽細節

- [ ] `f54` **［終端機］** 在護理端停留於「即時總覽」，然後到終端機按 Ctrl+C 停掉後端。
  → 右上標籤由「即時連線中」變成「**串流中斷，改為定時更新**」（灰底）。
- [ ] `f55` **［終端機］** 重新啟動後端（`npm run dev:api`）。
  → 標籤自動變回「即時連線中」，**不需要重新整理頁面**。串流有指數退避重連，另外還有 15 秒一次的輪詢當保險。
- [ ] `f56` **［護理端］** 把病人端分頁關掉，等 90 秒以上再看總覽。
  → 平板欄的綠點變**灰點**、文字變「未回應」。門檻由 `DEVICE_ONLINE_THRESHOLD_SECONDS` 控制（預設 90 秒）。
- [ ] `f57` **［護理端］** 同時讓兩位病人送出求助，觀察排序。
  → 有待處理求助的病人一律排在最前面，其餘依時段與病歷號排序 — 順序穩定，不會因為即時更新而跳動。
- [ ] `f58` **［護理端］** 看療程進度條。
  → 進度 = 已綁定時間 ÷ 療程時間上限，純時間換算。**這不是臨床進度**，只是提示這台平板綁多久了。

> **補充｜總覽的日期**
>
> 即時總覽只看**當日**（快取層只服務當天，跨日會自動重建）。後端 `GET /api/overview?date=YYYY-MM-DD` 支援查其他日期並改為直讀資料庫，但護理端畫面上還沒有日期選擇器。

### 6.12 稽核軌跡（FR-S02）

- [ ] `f59` **［護理端］** 展開「全部動作」下拉選單。
  → 共 **68 種**動作，皆為中文標籤（迭代 1 的 26 種、迭代 2 的 6 種症狀／求助事件，加上迭代 3～6 陸續新增的功能開關、備份、AI 呼叫、臨床數值匯入、求助結案、班表、輪播等事件）。
- [ ] `f60` **［護理端］** 在「平板序號篩選」輸入 `TB-01`。
  → 只列出與該平板相關的事件（綁定、解除、Session 送達、清除、MDM 操作、症狀回報、求助）。
- [ ] `f61` **［護理端］** 看「操作者」欄的三種型態。
  → 護理師顯示「工作ID（姓名）」；平板送出的事件顯示「平板 TB-01」；系統自動事件顯示 `SYSTEM`。病人**沒有帳號**，所以病人的動作一律記在裝置名下。
- [ ] `f62` **［護理端］** 翻到第 2 頁。
  → 每頁 50 筆，頁碼與總筆數顯示正確。
- [ ] `f63` **［護理端］** 以一般護理師身分嘗試直接開 `http://localhost:5173/audit`。
  → 頁面可以開，但資料載入時出現紅色橫幅「權限不足，缺少：audit:read」— 授權判斷在**後端**，不是靠前端藏連結。

## 07 · 自動化驗收腳本

手動走完之後，用這幾支腳本做回歸。它們會建立少量以 `VERIFY` / `V2` / `V4` / `V5` / `V6` 標記的合成資料，並逐項斷言。

> **注意｜前置**
>
> 後端**必須先啟動**（`npm run dev` 或 `npm run dev:api`），腳本才連得上。
> `verify:iteration3` 與 `verify:iteration4` 另外**要求 `.env` 有 `BACKUP_DIR`**（`a12`）：兩支都會做一次線上備份再用還原檔驗證，沒設定會直接判定該步驟失敗，而不是略過。

```bash
npm run verify:acceptance   # Iteration 1
npm run verify:iteration2   # Iteration 2
npm run verify:iteration3   # 迭代 3（需設定 BACKUP_DIR）
npm run verify:iteration4   # 迭代 4（需先 db:seed，並設定 BACKUP_DIR）
npm run verify:iteration5   # 迭代 5
npm run verify:iteration6   # 迭代 6
```

- [ ] `g1` **［終端機］** 執行 Iteration 1 驗收。
  → 13 個步驟全部 ✓，最後顯示「驗收結果：全部通過 ✓」。涵蓋登入、審核、排班、指派、重複綁定阻擋、Session 跨裝置隔離、解除、稽核、登出撤銷。
- [ ] `g2` **［終端機］** 執行 Iteration 2 驗收。
  → 11 個步驟全部 ✓。涵蓋問卷送出、即時推送、求助、分派規則、趨勢摘要、接手處理、離線補傳與冪等去重、下機收斂、稽核事件。
- [ ] `g3` **［終端機］** 看 Iteration 2 第 2 步印出的延遲基準，以及結尾有沒有「環境相關觀察」。
  → 改用本機 SQLite 之後**不應該再出現「環境相關觀察」**（端到端約 10 ms，遠低於 5 秒門檻）。若又冒出來，代表資料庫變慢了 — 先確認 `DATABASE_URL` 指的是本機磁碟上的檔案，不是網路磁碟機或雲端同步資料夾。
- [ ] `g4` **［終端機］** 執行迭代 4 驗收（AI 總開關須先關閉）。
  → 18 個步驟全部 ✓。第 12 步會用備份還原檔另起一個 `LLM_PROVIDER=lab` 的後端（port 3998）驗證「只改設定就能切換供應者、真實病人資料被擋下」，約需一分鐘；腳本結束前會把 AI 總開關關回去。
- [ ] `g5` **［終端機］** 執行迭代 5 驗收。
  → 15 個步驟全部 ✓。涵蓋結案的結構化欄位擋下、確認與到達床邊兩個時間、再發標示、病人端已處理、班表建立與匯入、一般護理師只看得到自己的班、勞動條件擋下並說明違反哪一條、成效基準建檔與估算值標示、稽核紀錄。腳本會把它改過的營運參數改回原值。
- [ ] `g6` **［終端機］** 執行迭代 6 驗收。
  → 15 個步驟全部 ✓。涵蓋輪播三層的開關、卡片不含姓名與病歷號、第三層每筆都有資料時間、
  問卷到期時輪播讓路、求助反應時間與非輪播狀態的比較、Excel 整批退回與逐列指出問題、
  重複上傳擋下、欄位對應改名後仍可匯入、綁定解除後輪播斷源、瀏覽事件不含可識別內容。
  腳本會把它改過的三個輪播開關與閒置門檻改回原值。
- [ ] `g7` **［終端機］** 執行迭代 3 驗收。
  → 十個步驟全部 ✓，結尾另提醒兩項需人工執行的實機驗收。涵蓋 SQLite 必開設定與唯讀連線、資料庫檔案位置、功能開關、模型供應者閘道、臨床資料來源介面層、唯讀角色、三處併發、備份與還原、零院外連線、稽核四類事件；逐項的手動測試在[迭代 3 分冊](iteration-3.md)。

## 08 · 收工 — 正確關閉與重新啟動

測試告一段落時，把服務關乾淨比想像中重要：Windows 上 `Ctrl+C` 有機會沒把子行程一起帶走，下次啟動就會撞 `EADDRINUSE`，而且你會以為是程式壞了。

### 正常關閉

- [ ] `j1` **［終端機］** 回到執行 `npm run dev` 的那個終端機視窗，按一次 **Ctrl+C**。
  → concurrently 會一併停掉後端與兩個 Vite，三色 log 停止輸出，提示字元回到你手上。後端有註冊 shutdown hook，會**正常中斷資料庫連線**後才結束，不是硬砍。
- [ ] `j2` **［終端機］** 如果你另外開了視窗跑 Prisma Studio（§06.7），**那個視窗要各按一次 Ctrl+C**。
  → port 5555 釋出。它是獨立行程，不會被 `npm run dev` 的 Ctrl+C 帶走。
- [ ] `j3` **［病人端］** 關掉病人端與護理端的瀏覽器分頁。
  → 病人端每 3 秒輪詢一次、護理端持有一條 SSE 長連線。不關不會壞（後端一起來它們會自動接回），但排查問題時這些背景請求會干擾你判讀 log。

### 確認真的關乾淨

這一步別跳過。四個 port 分別是：後端 `3000`、護理端 `5173`、病人端 `5174`、Prisma Studio `5555`。

*PowerShell*

```bash
Get-NetTCPConnection -LocalPort 3000,5173,5174,5555 -State Listen -ErrorAction SilentlyContinue |
  Select-Object LocalPort, OwningProcess
```

*Git Bash / cmd.exe*

```bash
netstat -ano | findstr /R /C:":3000 " /C:":5173 " /C:":5174 " /C:":5555 "
```

- [ ] `j4` **［終端機］** 執行上面任一版本。
  → **沒有任何輸出＝已經關乾淨**。若列出 port 與 PID，代表有行程沒被帶走（Windows 上這其實蠻常見的）。
- [ ] `j5` **［終端機］** 有殘留的話，用上一步查到的 **PID** 精準結束它。
  → 再查一次應該就沒有輸出了。

*用 PID 結束（把 33244 換成你查到的數字）*

```bash
# PowerShell
Stop-Process -Id 33244 -Force

# cmd.exe
taskkill /PID 33244 /F

# Git Bash 要用雙斜線
taskkill //PID 33244 //F
```

> **地雷｜不要一次砍光所有 node**
>
> `Get-Process node | Stop-Process -Force` 這種寫法**非常危險**。一般開發機上通常同時有十幾個 node 行程 — 編輯器、語言伺服器、其他專案、甚至 Claude Code 本身都可能是 node。**一定要先查出 port 對應的 PID，再針對那個 PID 結束。**

### 關掉之後，哪些東西還在

| 項目 | 關閉服務後會怎樣 |
|---|---|
| **所有資料**（帳號、病人、排班、綁定、問卷、求助、稽核） | 都在 `DATABASE_URL` 指向的 SQLite 檔案裡，**完全不受影響**。要搬移或備份時，主檔與 `-wal`／`-shm` 是一組，不要只拿主檔。 |
| **進行中的綁定** | **不會**因為關掉服務而失效，資料庫裡仍是 `ACTIVE`。下次啟動照常運作；若一直沒人管，超過 `BINDING_MAX_HOURS`（預設 6 小時）後，背景掃描會自動讓它逾時失效。 |
| **病人端未送出的離線回報** | 存在瀏覽器的 IndexedDB，**關掉分頁不會遺失**。下次用同一台平板開啟、且該筆綁定仍有效時會自動補傳。但綁定若已被解除，補傳會被拒絕，資料會在清除流程中一併移除。 |
| `.device-keys.json` | 留在 `apps/api/`，下次還能用。除非你重跑 `db:seed` 或重置資料庫。 |
| **登入權杖** | 存在瀏覽器 localStorage，**下次開啟仍是登入狀態**（除非超過 `JWT_EXPIRES_IN`，預設 8 小時）。 |

### 收工前建議做的兩件事

- [ ] `j6` **［護理端］** 看即時總覽，把還在「療程進行中」的綁定按「下機結束」。
  → 不是必要步驟，但下次進來時資料狀態乾淨，也比較貼近真實流程（病人下機才離開）。
- [ ] `j7` **［病人端］** 看病人端有沒有「尚有 N 筆症狀回報等待傳送」。
  → 若有，先確認網路正常、等它顯示「已將 N 筆離線暫存的症狀回報送出給護理站」再關 — 否則那筆資料可能會隨著綁定解除被清掉。

### 下次重新啟動

- [ ] `j8` **［終端機］** 在專案根目錄執行 `npm run dev` 就好。
  → **不需要**重跑 `prisma:migrate` 或 `db:seed` — 資料都還在。`build:shared` 也不用手動跑，`npm run dev` 會自動先跑一次。
- [ ] `j9` **［終端機］** 若啟動時出現 `EADDRINUSE :::3000`。
  → 就是上次沒關乾淨。回到本節「確認真的關乾淨」，查 PID 然後結束它，再啟動一次。

> **補充｜只重新啟動後端**
>
> 改了後端程式碼時，`npm run dev` 用的是 `nest start --watch`，會**自動重新編譯並重新啟動**，不必手動關。此時護理端的即時總覽會短暫顯示「串流中斷，改為定時更新」，後端起來後自動變回「即時連線中」。

## 09 · API 附錄

後端啟動時會印出完整路由清單，目前共 **107 條**，下表逐條列出。UI 沒有暴露的功能（取消排班、指定日期查總覽、跨裝置 Token 測試）都要從這裡打。

### 怎麼帶憑證

護理端用 **JWT**；病人端平板用**三個標頭**（序號＋金鑰，寫入資料時再加 Session Token）。

*取得護理端權杖並呼叫（bash）*

```bash
TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"workId":"ADMIN001","password":"你的密碼"}' | node -pe 'JSON.parse(require("fs").readFileSync(0)).accessToken')

curl -s http://localhost:3000/api/overview -H "Authorization: Bearer $TOKEN"
```

*平板端呼叫*

```bash
curl -s http://localhost:3000/api/device/session \
  -H 'x-device-serial: TB-01' \
  -H 'x-device-key: <金鑰>'

# 寫入資料時再加上 Session Token
curl -s http://localhost:3000/api/device/symptom-reports/mine \
  -H 'x-device-serial: TB-01' \
  -H 'x-device-key: <金鑰>' \
  -H 'x-session-token: <上一步回應中的 sessionToken>'
```

> **注意｜PowerShell 使用者**
>
> 上面兩段的 `curl` 全部要改成 `curl.exe`，否則 PowerShell 會用 `Invoke-WebRequest` 的別名去解析，參數對不上。取權杖那段用了 bash 語法（`$(...)`、單引號），在 PowerShell 建議改用 Git Bash 執行，或改寫成 `Invoke-RestMethod`。

### 路由表

| 方法／路徑 | 需要的權限 | 說明 |
|---|---|---|
| `POST /api/auth/register` | 公開 | 護理師註冊申請 |
| `POST /api/auth/login` | 公開 | 登入，回傳 JWT |
| `POST /api/auth/logout` | 登入態 | 撤銷本次權杖 |
| `POST /api/auth/change-password` | 登入態 | 撤銷該帳號所有權杖 |
| `GET /api/auth/me` | 登入態 | 取得自己的檔案與權限清單 |
| `GET /api/nurses/pending` | nurse:approve | 待審核清單 |
| `GET /api/nurses` | nurse:approve | 全部帳號 |
| `POST /api/nurses/:id/approve` | nurse:approve | 核准 |
| `POST /api/nurses/:id/reject` | nurse:approve | 駁回（需 reason） |
| `PATCH /api/nurses/:id/permissions` | nurse:grant-permission | 調整角色／審核權限 |
| `GET /api/patients` | 登入態 | 病人清單 |
| `POST /api/patients` | patient:manage | 建立病人 |
| `GET /api/devices` | 登入態 | 平板清單（指派時需要） |
| `POST /api/devices` | device:manage | 註冊平板，回傳一次性金鑰 |
| `GET /api/devices/mdm/console` | device:manage | 模擬 MDM 控制台狀態 |
| `POST /api/devices/:id/mdm/enroll` | device:manage | MDM 註冊 |
| `POST /api/devices/:id/mdm/unenroll` | device:manage | MDM 解除註冊 |
| `POST /api/devices/:id/mdm/lock` | device:manage | 遠端鎖定 |
| `POST /api/devices/:id/mdm/unlock` | device:manage | 解除鎖定 |
| `POST /api/devices/:id/mdm/kiosk` | device:manage | 設定 Kiosk 網址 |
| `GET /api/treatment-sessions?date=` | 登入態 | 當日排班 |
| `POST /api/treatment-sessions` | schedule:manage | 建立排班 |
| `POST /api/treatment-sessions/:id/cancel` | schedule:manage | **取消排班（UI 未提供）** |
| `GET /api/bindings` | 登入態 | 近期綁定紀錄 |
| `POST /api/bindings` | binding:manage | 指派平板 |
| `POST /api/bindings/:id/release` | binding:manage | 解除（reason: MANUAL_RELEASE ／ NORMAL_DISCHARGE） |
| `GET /api/device/session` | 裝置金鑰 | 平板輪詢綁定狀態 |
| `GET /api/device/session/context` | 裝置＋Token | 取得本次綁定病人資料 |
| `POST /api/device/session/cleared` | 裝置金鑰 | 回報本地資料已清除 |
| `POST /api/device/symptom-reports` | 裝置＋Token | 送出症狀問卷 |
| `GET /api/device/symptom-reports/mine` | 裝置＋Token | 本次綁定已送出的回報 |
| `POST /api/device/help-requests` | 裝置＋Token | 送出求助 |
| `GET /api/device/help-requests/mine` | 裝置＋Token | 本次綁定的求助狀態 |
| `GET /api/symptom-reports/treatment-session/:treatmentSessionId` | patient:monitor | 某次療程的所有回報 |
| `GET /api/symptom-reports/trend/:patientId` | patient:monitor | 症狀趨勢摘要 |
| `GET /api/help-requests?status=` | patient:monitor | 通報清單（含趨勢摘要） |
| `GET /api/help-requests/:id` | patient:monitor | 單筆通報 |
| `POST /api/help-requests/:id/acknowledge` | help-request:handle | 接手 |
| `POST /api/help-requests/:id/resolve` | help-request:handle | 處理完成 |
| `POST /api/help-requests/:id/cancel` | help-request:handle | 取消 |
| `GET /api/overview?date=` | patient:monitor | 多病人即時總覽 |
| `GET /api/realtime/stream` | patient:monitor | 即時串流（SSE，不是一般的 JSON 回應） |
| `GET /api/audit-logs` | audit:read | 稽核軌跡（可篩 action／deviceSerialNo／日期／分頁） |
| `GET /api/health` | 公開 | 健康檢查，不需要任何憑證 |
| `GET /api/feature-flags/enabled` | 登入態（唯讀角色亦可） | （迭代 3）目前開著的功能開關，前端用來決定要不要畫出入口 |
| `GET /api/feature-flags` | feature-flag:manage | （迭代 3）十個開關的完整狀態與開啟條件 |
| `PUT /api/feature-flags/:key` | feature-flag:manage | （迭代 3）切換開關（需 reason；AI 那兩項另需 system:configure） |
| `GET /api/ai/status` | system:configure | （迭代 3）目前供應者、健康檢查與可用模型 |
| `POST /api/ai/test-invocations` | system:configure＋AI 開關 | （迭代 3）介面層連線測試，走與正式功能相同的閘道 |
| `GET /api/ai/invocations` | ai-invocation:read（唯讀角色亦可） | （迭代 3）AI 呼叫紀錄 |
| `GET /api/ops/database` | backup:manage | （迭代 3）SQLite 必開設定與檔案大小（**不含檔案路徑**） |
| `GET /api/backups` | backup:manage | （迭代 3）備份歷程 |
| `POST /api/backups` | backup:manage | （迭代 3）立即線上備份（`VACUUM INTO`） |
| `POST /api/clinical-values/manual-entries` | clinical-value:manage | （迭代 3）人工輸入臨床數值，整批全有或全無 |
| `GET /api/clinical-values` | patient:monitor | （迭代 3）臨床數值清單（含被覆蓋的前一版） |
| `GET /api/clinical-value-imports` | clinical-value:manage | （迭代 3）最近的匯入批次 |
| `GET /api/ai-jobs/:id` | patient:monitor | （迭代 4）背景工作進度 |
| `GET /api/education/patients/:patientId` | patient:monitor | （迭代 4）衛教內容、完成紀錄、測驗與知識點 |
| `POST /api/education/contents` | education:manage＋AI 開關 | （迭代 4）產生個人化衛教，排入佇列 |
| `POST /api/education/discharge-summaries` | education:manage＋AI 開關 | （迭代 4）產生離院衛教重點，排入佇列 |
| `POST /api/education/contents/:id/approve` | education:manage | （迭代 4）核可，病人端可見 |
| `POST /api/education/contents/:id/reject` | education:manage | （迭代 4）退回（需 note） |
| `GET /api/device/education` | 裝置＋Token | （迭代 4）已核可的衛教內容與適性測驗題目 |
| `POST /api/device/education/contents/:id/complete` | 裝置＋Token | （迭代 4）我看完了 |
| `POST /api/device/quiz-attempts` | 裝置＋Token | （迭代 4）送出測驗作答 |
| `POST /api/device/feedback-responses` | 裝置＋Token | （迭代 4）送出情緒／滿意度回饋 |
| `GET /api/device/feedback-responses/mine` | 裝置＋Token | （迭代 4）本次療程是否已填 |
| `GET /api/psychosocial/overview` | patient:monitor | （迭代 4）心理社會彙總 |
| `GET /api/psychosocial/patients/:patientId` | patient:monitor | （迭代 4）單一病人的回饋與暫定規則比對 |
| `GET /api/adequacy/patients/:patientId` | patient:monitor | （迭代 4）透析適足性趨勢 |
| `POST /api/adequacy/calculations` | clinical-value:manage | （迭代 4）計算 URR 與 spKt/V |
| `GET /api/nursing-records?treatmentSessionId=` | patient:monitor | （迭代 4）護理記錄清單 |
| `GET /api/nursing-records/:id` | patient:monitor | （迭代 4）單筆護理記錄 |
| `POST /api/nursing-records/events` | nursing-record:write | （迭代 4）以快速範本建立事件記錄 |
| `POST /api/nursing-records/prefills` | nursing-record:write | （迭代 4）依病人自報建立預填草稿 |
| `POST /api/nursing-records/:id/ai-draft` | nursing-record:write＋AI 開關 | （迭代 4）AI 草擬初稿，排入佇列 |
| `POST /api/nursing-records/:id/sign` | nursing-record:write | （迭代 4）簽核，之後不可修改 |
| `GET /api/sop/documents` | sop:query | （迭代 4）收錄的文件 |
| `POST /api/sop/queries` | sop:query | （迭代 4）SOP／藥品劑量查詢 |
| `POST /api/help-requests/:id/arrive` | help-request:handle | （迭代 5）登記到達床邊時間，只能登記一次 |
| `GET /api/help-requests/follow-ups/open` | patient:monitor | （迭代 5）待追蹤事項清單 |
| `POST /api/help-requests/follow-ups/:followUpId/close` | help-request:handle | （迭代 5）結束追蹤事項（完成或取消） |
| `GET /api/help-resolution-options` | 登入態 | （迭代 5）結案畫面用：只有啟用中的處理方式與處理結果 |
| `GET /api/help-resolution-options/all` | operational-setting:manage | （迭代 5）設定畫面用：含已停用者 |
| `PUT /api/help-resolution-options` | operational-setting:manage | （迭代 5）新增或停用一個選項（代號限大寫英數與底線） |
| `GET /api/operational-settings` | 登入態 | （迭代 5）營運參數現值（再發判定期間、勞動條件上下限、輪播門檻等） |
| `PUT /api/operational-settings/:key` | operational-setting:manage | （迭代 5）調整某一項營運參數（**理由必填**） |
| `GET /api/shifts?from=&to=` | 登入態 | （迭代 5）班表；一般護理師只讀得到自己的班 |
| `POST /api/shifts` | shift:manage | （迭代 5）手動建立班次，違反勞動條件即擋下 |
| `POST /api/shifts/imports` | shift:manage | （迭代 5）班表檔案匯入，整批全有或全無 |
| `POST /api/shifts/:id/beds` | shift:manage | （迭代 5）指定當班負責床位（整組取代，不累加） |
| `POST /api/shifts/:id/cancel` | shift:manage | （迭代 5）取消班次（轉為已取消，不從表上消失） |
| `GET /api/shifts/change-requests` | 登入態 | （迭代 5）調班申請；一般護理師只看得到自己送出的 |
| `POST /api/shifts/change-requests` | 登入態 | （迭代 5）提出調班申請，**只能對自己的班次** |
| `POST /api/shifts/change-requests/:id/decide` | shift:manage | （迭代 5）核准或駁回調班申請 |
| `GET /api/baselines/metrics` | 登入態 | （迭代 5）五條成效指標的定義與量測方式 |
| `GET /api/baselines/overview` | 登入態 | （迭代 5）建檔進度：還差幾項、哪幾項只有估算值 |
| `GET /api/baselines` | 登入態 | （迭代 5）歷次建檔紀錄（含已被取代的） |
| `POST /api/baselines` | baseline:manage | （迭代 5）建檔一條成效基準（背書者必填） |
| `GET /api/device/carousel` | 裝置＋Token | （迭代 6）本次綁定的輪播內容與行為參數 |
| `POST /api/device/carousel/view-events` | 裝置＋Token | （迭代 6）回報瀏覽事件（只有卡片種類、停留時間、點開次數） |
| `GET /api/carousel/items` | carousel-content:manage | （迭代 6）第二層內容清單（含已下架） |
| `POST /api/carousel/items` | carousel-content:manage | （迭代 6）新增或修改第二層內容（帶 id 為修改） |
| `GET /api/carousel/view-stats?days=` | carousel-content:manage | （迭代 6）依卡片種類彙總的瀏覽狀況 |
| `POST /api/clinical-values/file-imports` | clinical-value:manage | （迭代 6）Excel／CSV 匯入，整批全有或全無 |
| `GET /api/import-field-mappings` | clinical-value:manage | （迭代 6）匯入欄位對應設定 |
| `PUT /api/import-field-mappings/:targetField` | clinical-value:manage | （迭代 6）修改某一個欄位的對應名稱或啟停用 |

> 表中各迭代的操作步驟分別在[迭代 3](iteration-3.md)、[迭代 4](iteration-4.md)、[迭代 5](iteration-5.md)、[迭代 6](iteration-6.md) 四本分冊。
> 本表逐條對齊後端啟動時印出的路由清單（目前 107 條）；日後新增端點時，以那份清單為準回頭補這張表。

- [ ] `h1` **［終端機］** 用 curl 測「取消排班」，先建立一筆有綁定的排班再取消。
  → 409「此排班仍有進行中的裝置綁定，請先解除綁定」。解除後再取消則成功，狀態變 `CANCELLED`。
- [ ] `h2` **［終端機］** 用 curl 查昨天的總覽。
  → 回應中 `fromCache` 為 `false`（非當日改為直讀資料庫）；查今天則為 `true`。

## 10 · 疑難排解

| 症狀 | 原因與處理 |
|---|---|
| PowerShell 執行 curl 噴「找不到符合參數名稱 i 的參數」 | PowerShell 的 `curl` 是 `Invoke-WebRequest` 的別名。改打 `curl.exe`，或改用 Git Bash。 |
| `npm install` 出現 EPERM／權限不足 | ① 關掉還在跑的 node 行程與編輯器；② 專案不要放在 OneDrive 等同步資料夾；③ 把資料夾加入防毒排除清單。仍失敗再以系統管理員身分開 PowerShell 重跑。 |
| 用 .env 的帳密登入卻說「工作ID 或密碼錯誤」 | 最常見是**中文輸入法把 `!` 打成全形 `！`** — 改用複製貼上。其次是你已經走過強制變更密碼那步（.env 的初始密碼從此失效）。用 §03 的 node 指令可直接判斷是哪一種。 |
| 後端起不來，噴 `Error code 14: Unable to open the database file` | **多半是跳過了 `npm run prisma:migrate` 就直接 `npm run dev`。** 建資料夾與建檔案是 migration 在做，後端自己不會建。先補跑 `a7`、`a8`（`a13` 確認檔案出現）再啟動。其次才是 `DATABASE_URL` 打錯：要是**絕對路徑**、用正斜線、含空白時整串加引號。 |
| 後端起來了，但護理端怎麼登都不行，log 有黃字說未設定 `SUPER_ADMIN_*` | `.env` 漏填 `SUPER_ADMIN_WORK_ID` 或 `SUPER_ADMIN_INITIAL_PASSWORD`，資料庫裡沒有任何帳號。補進 `.env` 後重新啟動即可，**資料庫不必重建**（那段邏輯只在「資料庫尚無任何使用者」時執行）。 |
| 病人端一直「裝置尚未佈建」，但金鑰是從 `.device-keys.json` 複製的 | 那份檔案對應的是**最後一次 seed 的那顆資料庫**。中途換過 `DATABASE_URL` 或在別的資料庫上跑過 `prisma:migrate`／`db:seed`，檔案就被覆蓋了，明文金鑰拿不回來（資料庫只存雜湊）。在「裝置管理」重新註冊一台取得新金鑰（`f20`），或照 §11 重置資料庫。 |
| 每個動作都要等好幾秒 | 本機 SQLite 單次往返約 2 ms，慢下來多半是資料庫檔案放在網路磁碟機、OneDrive 之類的同步資料夾，或被防毒即時掃描。換到本機磁碟並加入防毒排除清單。 |
| `prisma migrate reset` 噴 `database disk image is malformed`，資料庫剩 0 byte | 清空成功、建表失敗（多半是後端還開著，WAL 檔被抓住）。照 **§11** 的地雷框：關乾淨 → 把 `hd.db` 與 `-wal`／`-shm` 三個一起刪 → `prisma:deploy` + `db:seed` 重建。 |
| 後端 log 出現 `SQLITE_BUSY` | 有第二個行程在寫同一個檔案（多半是上次沒關乾淨的後端，或開著的 Prisma Studio）。照 §08 查出 PID 收乾淨，後端是唯一的寫入者。 |
| `EADDRINUSE :::3000`（或 5173／5174） | 上次沒關乾淨，有殘留行程佔著 port。照 **§08** 查出 PID 再精準結束；**不要**一次砍光所有 node 行程。 |
| 前端出現 CORS 錯誤 | `CORS_ORIGINS` 沒包含前端網址。預設是 `http://localhost:5173,http://localhost:5174`。 |
| 操作到一半一直跳 401 | 權杖過期（`JWT_EXPIRES_IN` 預設 8h），或你剛變更密碼／被降權 — 兩者都會主動撤銷既有登入階段。重新登入即可。 |
| 病人端一直停在「裝置尚未佈建」 | 序號或金鑰不對，或該平板還沒在後端註冊。用 `.device-keys.json` 的值，或在護理端重新註冊一台取得新網址。 |
| 指派平板的下拉選單是空的 | 平板需**同時**符合：已 MDM 納管、未被鎖定、未被其他病人佔用。seed 出來的 15 台預設**未納管**。 |
| 改了 `packages/shared` 沒生效 | 要重跑 `npm run build:shared`。`npm run dev` 啟動時會自動跑一次。 |
| 稽核軌跡少了某筆記錄 | 稽核寫入失敗**不會中斷**主流程，只在後端 log 留下 error。去終端機找 `稽核軌跡寫入失敗`。 |

## 11 · 重置到乾淨狀態

> **地雷｜會刪除所有資料**
>
> 下面的指令會**清空整個資料庫**並重建。只在測試環境執行。

反覆測試會累積大量帳號、病人與平板。想從乾淨狀態重走一遍手冊時：

```bash
cd apps/api
npx dotenv -e ../../.env -- prisma migrate reset
```

- [ ] `i1` **［終端機］** 先照 §08 把服務關乾淨，再執行重置（會要求你打字確認）。
  → 刪除所有資料表 → 重新套用五個 migration → **自動重跑 seed**（8 位病人、15 台平板、新的 `.device-keys.json`）。
  **後端沒關就跑，多半會停在下面那個 `malformed`。**
- [ ] `i4` **［終端機］** 重置後確認資料庫檔案不是 0 byte。
  → `DATABASE_URL` 指向的位置有一個數百 KB 的 `hd.db`。**若看到 0 byte，代表 `i1` 中途失敗了**，照下方的地雷框收拾。
- [ ] `i2` **［終端機］** 重新啟動後端。
  → 因為資料庫已無任何使用者，後端會依 `.env` **重新建立 SUPER_ADMIN**，並且 `passwordChangeRequired` 回到 true — 下次登入會再次強制變更密碼。
- [ ] `i3` **［護理端］** 從 §03 重新走一遍。
  → 全新環境，所有步驟的預期結果與手冊一致。

> **地雷｜重置噴 `database disk image is malformed`，資料庫變成 0 byte**
>
> `migrate reset` 是**兩段動作**：先清空、再建表。撞到殘留的 `-wal`／`-shm` 檔時，它會清完才失敗，
> 於是你得到一個 **0 byte 的 `hd.db`**——舊資料已經沒了，新的也還沒建起來。看起來很嚇人，其實只是做到一半。
>
> 最常見的成因是**後端還開著**（SQLite 只容許一個寫入者，WAL 檔還被它抓著），
> 其次是上一次不正常結束留下的 WAL 檔。收拾方式固定三步，資料反正已經沒了，刪掉沒有損失：
>
> ```bash
> # ① 先照 §08 確認 3000／5173／5174／5555 都沒有行程監聽
> # ② 把主檔與兩個附屬檔一起刪掉（路徑換成你的 DATABASE_URL）
> rm -f /d/hd-data/hd.db /d/hd-data/hd.db-wal /d/hd-data/hd.db-shm
> # PowerShell：Remove-Item D:\hd-data\hd.db*
>
> # ③ 回專案根目錄重建（不必再跑 reset）
> npm run prisma:deploy -w @hd/api
> npm run db:seed
> ```
>
> 跑完的結果與 `i1` 成功時完全相同：五個 migration、8 位病人、15 台平板、新的 `.device-keys.json`。
> **三個檔案一定要一起刪**，只刪主檔的話，殘留的 WAL 會再讓下一次建表失敗一次。

> **補充｜你目前這個資料庫**
>
> 2026-09-10 改用本機 SQLite 時是從零開始建立的，Supabase 上的舊資料依《資料庫使用規範》第 15 條不遷移、直接捨棄。之後殘留的少量資料是驗收腳本留下的（工作ID 以 `VERIFY`／`V2`～`V6` 開頭、病歷號以 `HD-VERIFY`／`HD-V2`／`HD-TEST-V3` 之類的前置字開頭），不影響功能。

## 12 · 附錄 — 資料庫位置、備份與還原

> **說明｜Supabase 已撤出**
>
> 測試資料庫曾放在 Supabase（2026-09-07 由孟買搬到東京）。2026-09-10 起改為開發者本機的 SQLite 檔案，Supabase 專案已於 2026-09-11 刪除。原本本節的「更換 Supabase 區域」步驟已不適用，改為以下內容。

決定連到哪個資料庫的只有 `.env` 的 `DATABASE_URL` 一條字串。換位置＝換一個新檔案，舊資料不會跟過來。

### 換一個新的資料庫檔案

- [ ] `k1` **［終端機］** 先照 §08 把服務關乾淨。
  → 3000／5173／5174／5555 都沒有行程監聽。後端是唯一的寫入者，換檔案前一定要先停。
- [ ] `k2` **［終端機］** 把 `.env` 的 `DATABASE_URL` 改成新的絕對路徑（專案目錄外、本機磁碟）。
  → 檔案不必事先存在。
- [ ] `k3` **［終端機］** 建表並灌測試資料。
  → 資料夾與檔案自動建出來，套用五個 migration、8 位病人、15 台平板，並**產生全新的** `apps/api/.device-keys.json`。舊的佈建網址全部作廢 — 這一步是**單向的**，原本那顆資料庫的平板明文金鑰不會留下副本（見 §01 `a13` 下方的地雷框）。要留著回頭用的話，先把舊的 `.device-keys.json` 另存一份再往下走。
- [ ] `k4` **［終端機］** 啟動並看後端 log。
  → 出現「已連線至 SQLite：journal_mode=WAL、foreign_keys=ON、busy_timeout=5000ms、synchronous=FULL」與「**已建立初始最高權限帳號**」。第一次登入會再次強制變更密碼。

*步驟 k3–k4*

```bash
npm run prisma:deploy -w @hd/api
npm run db:seed
npm run dev
```

### 線上備份與還原演練

- [ ] `k5` **［護理端］** 以護理站管理者登入 →「系統管理」→「資料庫與備份」→「立即備份」。
  → 備份歷程多一列「成功」，有檔名、大小與 SHA-256；稽核軌跡多一筆「資料庫線上備份」。
- [ ] `k6` **［終端機］** 把剛才那份備份還原到另一個路徑。
  → 依序印出雜湊比對相符、完整性檢查 ok、各資料表筆數。不會碰到使用中的資料庫。
- [ ] `k7` **［終端機］** 把 `DATABASE_URL` 暫時指向還原出來的檔案，重新啟動後端並登入。
  → 帳號、病人、稽核紀錄都在。演練完記得把 `DATABASE_URL` 改回正本。

*步驟 k6*

```bash
npm run db:restore -- --from <BACKUP_DIR 裡的備份檔> --to <新的絕對路徑> --sha256 <備份歷程上的雜湊>
```

> **地雷｜不要直接複製資料庫檔案**
>
> 服務執行中直接複製 `.db` 可能拿到不一致的狀態，而且 WAL 模式的 `-wal`／`-shm` 與主檔是一組，只拿主檔會漏資料。備份一律用上面的線上備份（`VACUUM INTO`），備份檔也不得放到雲端硬碟或個人隨身碟（《資料庫使用規範》8.1）。

---

## 13 · 迭代 3-6 分冊

迭代 3 之後的功能各自成冊。全部併進本頁會超過兩千行，翻起來反而找不到東西；分開之後每一冊都能單獨走完，前置條件寫在各冊開頭。

| 分冊 | 內容 | 項次 |
|---|---|---|
| [迭代 3 — 封閉網路化與資料層遷移](iteration-3.md) | SQLite 必開設定、零院外連線、唯讀角色、功能開關中心、模型供應者與臨床資料來源兩個介面層、備份與還原、常駐總覽螢幕、兩項實機驗收 | `l1`–`l55`、`m1`–`m11` |
| [迭代 4 — AI 輔助內容與護理記錄](iteration-4.md) | 護理記錄與 AI 草擬、衛教與適性測驗、心情與滿意度、透析適足性、SOP 查詢、背景佇列 | `n1`–`n57` |
| [迭代 5 — 求助處理紀錄、班表與成效基準](iteration-5.md) | 結案的結構化登記、兩個時間、再發標示、結果回饋、班表與床位分配、勞動條件硬性檢查、成效基準建檔 | `o1`–`o68` |
| [迭代 6 — 閒置輪播與檔案匯入](iteration-6.md) | 輪播三層與主線保護、細節頁、夜間模式、Excel／CSV 匯入與可設定的欄位對應、瀏覽事件 | `p1`–`p63` |

走的順序建議照迭代編號：後面的分冊會用到前面建立起來的東西（例如迭代 4 要先照迭代 3 分冊開啟 AI 總開關，迭代 6 的第三層要先有迭代 3 的臨床數值）。

**開始任何一本分冊之前，先把這四樣東西準備好**（四本分冊的「前置」都建立在這些上面）：

| 要準備的 | 在哪裡弄出來 |
|---|---|
| 最高權限帳號（SUPER_ADMIN） | `.env` 的 `SUPER_ADMIN_*`，後端首次啟動時自動建立（`a10`、`c1`–`c3`） |
| 一位一般護理師 `N001` | 註冊申請＋核准（`c4`–`c7`） |
| 一位**護理長**（例如 `N003`） | 另外申請一個帳號，核准後在「帳號審核 → 帳號與權限」用角色下拉選單改成「護理長／單位主管」（`f64`）。**迭代 3、5、6 都要它** |
| `.env` 的 `BACKUP_DIR` | `a12`。迭代 3 §3.8 與 `verify:iteration3`／`verify:iteration4` 沒有它都過不了 |

**兩件事請優先安排**：

- 迭代 3 的兩項實機驗收（[迭代 3 分冊](iteration-3.md) §3.12）**尚未執行**，需要實機與 4 小時。
- 迭代 6 有四項是自動化腳本驗不到、只能用手指確認的（[迭代 6 分冊](iteration-6.md) §6.3～§6.5、§6.9），輪播對病人開啟前請先走過。

---

## 版本歷程

| 定版 | 日期 | 異動 |
|---|---|---|
| [0916](https://94sh09sh19sh.github.io/hd-docs/0916/testing/manual-test-guide/) | 2026-09-16 | 拆成主手冊＋迭代 3～6 四本分冊（共 410 項），§13 改為分冊索引；補齊環境建置缺口、路由表補到 107 條，§11 補上重置失敗留下 0 byte 資料庫的收拾方式 |
| 0909 | 2026-09-09 | 首次定版 |

[← 回進度首頁](../index.md)
