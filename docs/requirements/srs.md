# 血液透析中心平板照護輔助系統 — 軟體需求規格書（SRS）

版本：v2.0　文件狀態：草案　適用讀者：FDE 工程團隊、醫院資訊室、護理部、法務／法規合規承辦人

---

## 0. 本版異動摘要（0910）

0909 版的功能邊界與部署假設在本週被大幅改寫。以下六項是與 v1.0 的實質差異，第 5 章之後的所有內容都以本表為準：

| # | 項次 | v1.0（0909） | v2.0（0910） |
|---|---|---|---|
| 1 | 三項高 SaMD 風險功能（FR-P03、FR-P05、FR-N06） | 標註高風險，實作規格書整條排除 | **納入實作範圍**，但受第 5.6 節 FR-R 控制措施約束，並設書面確認閘門 |
| 2 | 推播通知 | 系統元件之一，架構圖有推播中繼那一跳 | **整條移除**。所有資料不得離開醫院網路 |
| 3 | AI 模型 | 經 AI Proxy 呼叫外部 LLM 服務商 | **地端推論**（院內 128GB 伺服器）；過渡期用實驗室 API，僅得送合成資料 |
| 4 | 資料庫 | 測試用 Supabase，正式為醫院 MSSQL | **測試與正式皆為 SQLite** |
| 5 | 使用者角色 | 病人、護理師、護理站管理者、FDE | **新增**護理長／單位主管、醫院管理層（唯讀）、稽核角色 |
| 6 | 功能範圍 | 病人端 8 項、護理端 9 項、系統 4 項 | **新增**管理與營運（FR-M）、對外揭露（FR-X）、高風險控制（FR-R）三組 |

### 0.1 0919 異動摘要（第一次正式部署前）

本週要做的是**第一次正式部署**，因此本版的變動全部集中在「交付、管控、內容來源、介面呈現」四件事上，**功能邊界一條未動**。

| # | 項次 | 0916 | 0919 |
|---|---|---|---|
| 1 | 應用主機 | 院內 NVIDIA DGX Spark（ARM64、共用） | **院內另一台 Windows 工作站**（x86-64、64GB、2TB SSD ×2，仍與其他專案共用，院方確保資源足夠）。DGX Spark 只剩「透過 API 呼叫的地端推論端點」一個身分 |
| 2 | 平板管控（FR-S01） | MDM（Android Enterprise）整合 | **免 MDM**：PWA 裝在自包 WebView 外殼裡，Kiosk 靠 Android 內建螢幕固定 |
| 3 | 臨床數值來源（FR-S05） | 院方未定 API 或 Excel | **確定走 API**。`FileImportAdapter` 退為備援 |
| 4 | 首波開放範圍 | 未定義 | **僅少數護理師與病人**。成效基準量測（Q-01）順延至下週 |
| 5 | 三項高風險功能 | 排在迭代 7 | **尚未實作，第一次部署不提供服務**；改列下週任務 |
| 6 | 選項與題庫內容 | 多項「待護理部確認」的暫代值 | **院方授權先採用預設值**，並須在護理端系統設定提供更改途徑。內容見附錄 C |
| 7 | 護理端導覽 | 17 條連結一字排開 | **只留每日必用**，其餘收進下拉選單或關閉，版位可於系統設定調整（FR-S11） |
| 8 | 介面呈現 | 未規範 | **內部技術識別不得出現在任何使用者介面**（FR-S10） |
| 9 | 版本更新 | 未規範 | **更新不得破壞既有資料**，訂出強制備份與前滾式遷移（FR-S12） |

**一句話**：0910 改的是「要做什麼」，0912 改的是「跑在哪裡」，**0919 改的是「怎麼交到使用者手上」**。

---

## 1. 文件目的與範圍

本文件定義「血液透析中心平板照護輔助系統」之軟體需求，涵蓋病人端 15 台 Android 平板、護理端多裝置應用、後端服務、地端 AI 輔助功能、單位管理與營運分析，以及資料治理規範。

系統目標為在不取代任何臨床判斷與侵入性操作的前提下，將可數位化與可數位優化的護理工作流程予以實作（工作區塊盤點見《[FDE 數位化／數位優化評估報告](fde-assessment.md)》）。

本文件**不涵蓋**：透析機台參數整合、藥局／HIS 系統雙向寫入、透析機台硬體通訊協定（列為未來擴充項）。

**與其他文件的關係**：本文件是功能需求的來源（FR 編號出處）。資料治理的強制規則以《[資料庫使用規範](database-policy.md)》為準；實作範圍與技術棧決策以《[實作規格書](implementation-spec.md)》為準；部署形態、環境界線與交付方式以《[部署規範](deployment-spec.md)》為準（`DEP` 編號）。

---

## 2. 名詞定義與縮寫

| 詞彙 | 定義 |
|---|---|
| FDE | Forward Deployed Engineer，本專案之技術實作與現場迭代工程師 |
| PWA | Progressive Web App，具離線快取與安裝能力之網頁式應用 |
| SaMD | Software as a Medical Device，軟體醫療器材。本系統 FR-P03、FR-P05、FR-N06 三項可能觸及此範疇 |
| 裝置綁定 | 將特定病人身分與特定平板序號、當次療程時段建立一次性關聯之機制 |
| 護理站主控台 | 護理端應用中，具備排班管理、裝置指派、緊急總覽等管理功能之介面模組 |
| Session Token | 綁定後由後端核發、限時且限裝置使用之存取憑證 |
| 常駐總覽螢幕 | 護理站中不進入休眠、持續顯示即時總覽的固定螢幕。因不做推播，此為通報能被看見的必要條件 |
| 地端推論 | 語言模型跑在院內伺服器，輸入輸出全程留在院內網路 |
| 閒置輪播 | 病人端平板無操作時自動播放的資訊畫面 |
| 成效基準 | 系統啟用前，同一件事所花的時間／人力／出錯次數 |
| L1～L4 | 營運與績效資料的四個敏感度級別，定義見《資料庫使用規範》第 11 條 |
| **自包 WebView 外殼** | 病人端 PWA 的載入容器：一支只做全螢幕、憑證信任與行為限制的 Android App，不是瀏覽器（0919 新增） |
| **螢幕固定** | Android 5.0 起內建的功能，把畫面釘在單一 App 上，解除需輸入 PIN。本系統的 Kiosk 鎖定靠它，不靠 MDM（0919 新增） |
| **離線安裝包** | 正式環境的交付物：一份自帶 Node.js 執行期、已建置產物與 Prisma 引擎的壓縮檔，在無對外連線的主機上解壓即可啟動（0919 新增） |
| **首波試用** | 第一次正式部署後的開放範圍：少數護理師與少數病人，非全單位（0919 新增） |
| **需手持裝置的功能** | 必須由護理師隨身攜帶平板或手機才能在正確時點按下的功能（例如床邊按鈕）。首波試用因裝置不足一律關閉（0919 新增） |

---

## 3. 系統總覽

### 3.1 使用者角色

| 角色 | 說明 | 0910 變動 |
|---|---|---|
| 病人 | 使用病人端平板進行症狀回報、衛教學習、求助、瀏覽閒置輪播；**不具備任何登入或身分選擇操作** | 新增輪播瀏覽 |
| 護理師 | 使用護理端裝置進行排班指派、即時監看、記錄審閱簽核、緊急處置、求助結果登記 | 新增求助結果登記 |
| 護理站管理者 | 具備額外權限：裝置佈建狀態與 Kiosk 設定、帳號權限設定、稽核紀錄查詢、介面版位與功能開關設定 | **0919 改寫**：MDM 裝置管理改為裝置佈建狀態管理（FR-S01）；新增介面版位設定（FR-S11） |
| **護理長／單位主管** | 護理師排班與調動、班次層級（L2）與個人層級（L3）績效檢視、獎勵制度設定 | **新增** |
| **醫院管理層** | 唯讀存取單位聚合（L1）成效儀表板，不得存取任何病人層級資料 | **新增** |
| **稽核／法規角色** | 唯讀存取稽核軌跡與 AI 呼叫紀錄，用於法規查核；不具臨床操作權 | **新增** |
| FDE 工程師／系統管理者 | 系統部署、規則與模型設定、資料治理設定，**不具臨床資料存取權** | 不變 |

### 3.2 系統邊界

系統由病人端 PWA、護理端 PWA、管理端介面（與護理端共用應用，以權限區分）、後端 API Gateway、裝置綁定服務、地端 AI 推論服務、規則引擎、臨床資料匯入服務、對外報表服務，以及 SQLite 資料檔所組成。

**0910 的邊界變動**：

- **推播通知服務移除。** 即時通知改由院內 SSE 長連線提供，配合常駐總覽螢幕與音效提示。
- **外部 LLM 服務移除。** AI 推論在院內伺服器完成。過渡期的實驗室 API 為院外服務，因此**過渡期僅得送合成資料**。
- **新增規則引擎。** 三項高風險功能不經語言模型，改以可攤開的規則計算（FR-R01）。
- **資料層改為單一 SQLite 檔案**，即時狀態改由後端記憶體與資料庫共同承擔，不另設快取服務。

**0919 的邊界變動**：

- **地端推論服務移出系統邊界。** 它不再是「本系統跑在同一台機器上的另一個服務」，而是**院內另一台機器（DGX Spark）上的一個 HTTP 端點**。本系統只持有它的位址與模型識別字，不負責它的部署、載入與維運。
- **MDM 服務移出系統邊界。** 平板管控改走免 MDM 路線，系統內留下的只有「裝置佈建狀態的紀錄與稽核」，不再有任何對外的裝置管理通道（FR-S01）。
- **臨床資料來源確定為院方 API。** `HospitalApiAdapter` 成為正式來源，檔案匯入退為備援（FR-S05）。
- **新增一個病人端元件：自包 WebView 外殼 App。** 它不含任何業務邏輯，只負責載入 PWA、信任院內自簽憑證、限制行為邊界，並回報前景狀態（逸出偵測）。

---

## 4. 權限模型與裝置綁定

### 4.1 病人端不提供登入介面（維持 v1.0 設計）

**病人端平板不提供任何登入介面**（無帳號、密碼、生物辨識、或病人自選身分清單）。病人於平板上能存取的所有資料與功能，其存取權限之啟用，一律由護理師透過「裝置指派」動作觸發。理由：

1. 病人在透析情境下（可能虛弱、視力不佳、認知負荷高）不適合承擔身分驗證責任
2. 平板為機構共用裝置，若允許病人自行選擇／切換身分，會產生資料誤植給錯病人的風險
3. 從法規遵循角度，臨床資料的存取授權應可追溯到「哪一位護理師、何時、為哪一位病人核准」，而非病人自證身分

### 4.2 權限矩陣

| 功能 | 病人 | 護理師 | 護理站管理者 | 護理長 | 醫院管理層 | 稽核角色 |
|---|---|---|---|---|---|---|
| 症狀回報、求助 | 可（限已指派之當次療程） | 檢視 | 檢視 | 檢視 | 不可 | 不可 |
| 閒置輪播瀏覽 | 可 | 檢視內容設定 | 可設定 | 可設定 | 不可 | 不可 |
| 衛教內容瀏覽／測驗 | 可（已指派後） | 檢視完成度 | 檢視完成度 | 檢視完成度 | 不可 | 不可 |
| 求助處理結果登記 | 不可 | 可 | 可 | 可 | 不可 | 不可 |
| 護理記錄審閱簽核 | 不可 | 可 | 可 | 可 | 不可 | 不可 |
| 裝置-病人指派／解除 | 不可 | 可 | 可 | 可 | 不可 | 不可 |
| 多病人即時總覽 | 不可 | 可 | 可 | 可 | 不可 | 不可 |
| 高風險功能輸出檢視 | 不可 | 可 | 可 | 可 | 不可 | 不可 |
| 高風險功能開關 | 不可 | 不可 | 可 | 可 | 不可 | 不可 |
| 護理師排班與調動 | 不可 | 檢視自己的班 | 可 | 可 | 不可 | 不可 |
| L1 單位聚合儀表板 | 不可 | 可 | 可 | 可 | **可（唯讀）** | 可 |
| L2 班次層級 | 不可 | 不可 | 可 | 可 | 不可 | 可 |
| L3 個人層級 | 不可 | 只看自己 | 可（需開關開啟） | 可（需開關開啟） | 不可 | 可 |
| L4 稽核軌跡原始列 | 不可 | 不可 | 可 | 不可 | 不可 | **可** |
| 對外報表產生 | 不可 | 不可 | 可（需核准） | 可（需核准） | 不可 | 不可 |
| 裝置佈建狀態與 Kiosk 設定 | 不可 | 不可 | 可 | 不可 | 不可 | 不可 |
| 介面版位與功能開關設定（FR-S11） | 不可 | 不可 | 可 | 可 | 不可 | 不可 |
| 預設選項與題庫內容維護（附錄 C） | 不可 | 不可 | 可 | 可 | 不可 | 不可 |
| AI 規則／模型參數調整 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可（僅 FDE） |

### 4.3 裝置與病人綁定流程

**前置條件**：護理站已建立當日排班表（病人—療程時段對應）。

1. 病人到達透析中心，護理師於護理站主控台（或當班護理端裝置）選取當日排班中的病人
2. 護理師選擇欲指派之平板序號（清單選取、掃描病人手環條碼、掃描平板背面 QR 碼三者擇一）
3. 護理師確認指派後，請求送至後端**裝置綁定服務**
4. 裝置綁定服務核發一組**限時 Session Token**，綁定「病人 ID＋平板序號＋當次療程時段」三項，並透過安全通道送至該平板
5. 平板在收到有效 Session 前，僅顯示中性等待畫面（「請由護理人員為您開始使用」），不顯示任何病人可識別資訊、不提供輸入欄位
6. Session 啟用後，平板僅能存取「本次已綁定病人」之資料與功能，無法切換查看其他病人
7. Session 失效條件（三者擇一觸發）：正常下機流程、護理師手動提前解除、超過療程時間上限（預設 6 小時）自動失效
8. Session 失效後，平板本地暫存資料需清除，並回復至「等待指派」狀態
9. 每一次綁定與解除動作，須記錄操作護理師帳號、時間戳、病人 ID、平板序號，寫入稽核軌跡

### 4.4 例外情況

- 平板離線期間無法完成新綁定；若原本已有效 Session 而平板離線，症狀回報可暫存本地（IndexedDB），恢復連線後同步，但求助功能於離線期間需明確提示「目前無法即時通知護理站」
- 若同一病人短時間內被誤指派至兩台平板，系統應偵測並阻擋第二次綁定，提示「該病人已有進行中之綁定」
- **0910 新增**：Session 失效後，閒置輪播中已快取的第三層臨床數值須一併清除，不得殘留給下一位使用者

---

## 5. 功能性需求

編號規則：`FR-P` 病人端、`FR-N` 護理端、`FR-M` 管理與營運、`FR-X` 對外揭露、`FR-S` 系統與整合、`FR-R` 高風險功能控制。
優先權：P0 必要／P1 次要／P2 可延後。
「SaMD 風險」欄僅標示該功能是否涉及臨床判斷輸出；標為**高**者，必須同時滿足第 5.6 節全部 FR-R 條款。

### 5.1 病人端

| 編號 | 需求描述 | 優先權 | SaMD 風險 | 0910 |
|---|---|---|---|---|
| FR-P01 | 平板於未綁定狀態僅顯示中性等待畫面，無登入介面 | P0 | 無 | 不變 |
| FR-P02 | 已綁定後，提供結構化透析前症狀問卷（水腫、喘、發燒、出血等） | P0 | 無 | 不變 |
| FR-P03 | 依病人歷史體重增幅／血壓趨勢，標註當日風險提示供護理師參考 | P1 | **高** | **由排除改為納入**，受 FR-R01～R08 約束 |
| FR-P04 | 提供個人化衛教內容（依透析史、衛教完成紀錄動態生成），支援多語言 | P1 | 無 | 改由地端模型生成 |
| FR-P05 | 透析中定時彈出式症狀問卷，並可依歷史同時段症狀模式提前預警 | P1 | **高** | **由排除改為納入**，受 FR-R01～R08 約束 |
| FR-P06 | 提供求助按鈕，觸發時自動附帶近期症狀趨勢摘要給護理端 | P0 | 中（僅摘要呈現，非診斷） | 不變 |
| FR-P07 | 下機後提供個人化離院衛教重點與適性測驗，標記需加強衛教之知識點 | P1 | 無 | 不變 |
| FR-P08 | 情緒／滿意度回饋問卷，長期趨勢偏離時建議轉介社工 | P2 | 中 | 不變 |
| **FR-P09** | **閒置輪播**：平板無操作達設定門檻（預設 60 秒）後，自動輪播本次透析的重要資訊 | P1 | 無 | **新增** |
| **FR-P10** | 輪播卡片可點選展開細節頁；細節頁停留超過設定時間或無操作後自動退回 | P1 | 無 | **新增** |
| **FR-P11** | 輪播內容分三層（系統既有資訊／靜態衛教／院方臨床數值），每一則臨床數值須標示資料時間 | P1 | 無 | **新增** |
| **FR-P12** | 求助被護理師結案後，平板顯示「已處理」狀態與簡短結果，讓病人知道事情有被處理 | P2 | 無 | **新增** |
| **FR-P13** | 輪播不得影響主線功能：求助按鈕全程可見且位置固定；任何觸控立即中止輪播；症狀問卷到期時優先於輪播；輪播全程靜音 | **P0** | 無 | **新增** |

**FR-P09～P13 補充說明**：

- 輪播第一層（療程進度、今日已回報內容、衛教完成度）**不依賴任何外部資料來源**，應優先實作。
- 輪播第三層（乾體重、超過濾目標、抽血結果、Kt/V）依賴院方提供，**0919 確定走院方 API**，一律經 FR-S05 的介面層取得；端點暫時不通時顯示上一次成功取得的值並照常標示資料時間，不顯示空白。
- 輪播畫面中的病人可識別資訊**只到床號層級**，姓名與病歷號不得出現在輪播畫面（治療區內鄰床可見）。
- 晚班時段輪播應自動調暗或改為靜態顯示。
- **0919 補充（FR-P13 的版位規則）**：求助按鈕「位置固定」必須以**版位固定**實作，不是以「輪播時把按鈕的層級抬高」實作。按鈕在輪播中與非輪播中都佔據畫面上同一塊保留區域，輪播內容的可用範圍必須把那塊區域先扣掉。**只要輪播的字有任何一個被求助按鈕蓋住，這一條就算不通過**——蓋住的是字，代價卻是病人以為按鈕壞了。
- **0919 補充（FR-P02／P06／P07／P08 的內容來源）**：症狀問卷題目、求助類別、衛教題庫與回饋題目的**預設內容見附錄 C**，且全部須可在護理端調整（FR-S11）。

### 5.2 護理端

| 編號 | 需求描述 | 優先權 | SaMD 風險 | 0910 |
|---|---|---|---|---|
| FR-N01 | 護理站主控台支援當日排班建立與裝置指派（見 4.3） | P0 | 無 | 不變 |
| FR-N02 | 多病人即時狀態總覽儀表板，顯示各病人當前療程進度與警示提示 | P0 | 無 | 不變 |
| FR-N03 | 緊急通報接收，含病人症狀趨勢背景摘要，依嚴重度分級路由 | P0 | 中 | 分級依據限定為病人自選急迫度＋靜態分派表，不得摻入模型推論 |
| FR-N04 | 事件當下快速點選式記錄範本，事後由 AI 草擬敘述初稿供審閱 | P1 | 無（AI 僅草擬，護理師簽核） | 改由地端模型生成 |
| FR-N05 | 護理記錄自動預填草稿（依病人自報資料），護理師審閱簽核取代重新輸入 | P1 | 無 | 改由地端模型生成 |
| FR-N06 | 每月／每週抽血與 Hb／EPO 趨勢儀表板，標註軌跡偏離之病人並提供劑量調整參考建議 | P1 | **高** | **由排除改為納入**，受 FR-R01～R08 約束 |
| FR-N07 | 透析適足性（Kt/V、URR）自動計算與趨勢預測 | P1 | 中（計算無風險，預測部分適用 FR-R） | 不變 |
| FR-N08 | 心理社會風險趨勢彙總與轉介建議 | P2 | 中 | 不變 |
| FR-N09 | SOP／藥品劑量查詢介面，僅限醫院既有文件範圍內回答，須標出原文出處段落 | P1 | 中 | 補上出處標示要求 |
| **FR-N10** | **求助處理結果登記**：護理師結案時須以結構化欄位登記處理方式與結果，不得只留自由文字 | **P0** | 無 | **新增** |
| **FR-N11** | 同一病人同一類別求助於設定期間內再次發生時，於通報畫面標示「近期再發」與前次處理方式 | P2 | 無 | **新增** |
| **FR-N12** | **常駐總覽螢幕模式**：一種不進入休眠、字級放大、有聲音提示的全螢幕總覽模式，供護理站固定螢幕使用 | **P0** | 無 | **新增（推播移除後的必要補償）** |
| **FR-N13** | 高風險功能（FR-P03／P05／N06）之輸出，護理師須逐則標記「已檢視」及「是否採納」，未採納可選填原因 | P0 | 無 | **新增** |

**FR-N10 的結構化欄位**（實際選項須由護理部確認）：

| 欄位 | 內容 |
|---|---|
| 處理方式 | 分類清單（現場處置、通知醫師、調整機台設定、提供物品、協助如廁、心理安撫、轉介、其他） |
| 處理結果 | 分類清單（已解決、部分緩解、持續觀察、升級處理、病人自行取消、無法處理） |
| 是否需後續追蹤 | 是／否，若是則帶出追蹤事項 |
| 到達床邊時間 | 由護理師在床邊登記產生，**與「確認通報」的時間分開記錄**。**0919：此登記需要護理師隨身帶著裝置，首波試用期間關閉**（見 FR-S11），關閉期間該欄位留空而非以確認時間頂替 |
| 自由文字補充 | 選填，不取代上述結構化欄位 |

**為什麼「確認時間」與「到達床邊時間」要分開**：兩者的差距是資料品質的檢查點。若只記錄確認時間，一旦這個數字被用於績效，最省力的做法會變成「先按確認再慢慢走過去」，指標就失去意義。分開記錄讓這種情況看得見。

**0919：這一條暫時做不到，而且必須承認做不到。** 護理站只有一兩台桌機、護理師手上沒有平板，「到床邊才按」在物理上無法發生；硬留著這顆按鈕，得到的會是「回到護理站後補按」的假資料，比沒有資料更糟。因此首波試用期間**關閉這顆按鈕**，該欄位一律留空，並在成效指標中明確標示「本期間無到達時間資料」。等護理師手上有裝置時，在系統設定開啟即可，資料結構不必改。

**處理方式與處理結果的選項內容**：0919 起改由附錄 C 的預設清單提供，院方已授權採用，並可在護理端系統設定增刪改。

### 5.3 管理與營運（0910 新增，對應新增任務一之 1～3）

本節全部為新增。設計原則見《[FDE 評估報告](fde-assessment.md)》第五章，資料分級與存取限制見《[資料庫使用規範](database-policy.md)》第 11 條。

| 編號 | 需求描述 | 優先權 | 資料級別 |
|---|---|---|---|
| FR-M01 | 護理師班表建立、檢視與匯入（支援手動建立與檔案匯入兩種方式） | P0 | L2 |
| FR-M02 | 當班床位分配：指定每位當班護理師負責的床位 | P0 | L2 |
| FR-M03 | 臨時調動：有人請假時，依技能標記、連續工時、近期負荷產生候補人選排序供護理長選擇 | P1 | L3 |
| FR-M04 | 勞動條件硬性檢查：連續工時、班距、單日時數上限違反時**擋下儲存**並說明違反哪一條，非僅提示 | **P0** | L2 |
| FR-M05 | 工作量實測儀表板：依既有稽核軌跡呈現各班次的求助件數、回應時間分布、尖峰時段 | P1 | L2 |
| FR-M06 | 成效儀表板（L1）：單位聚合指標，供醫院管理層唯讀存取 | P1 | L1 |
| FR-M07 | 成效基準建檔：提供介面讓護理長輸入系統啟用前的人工量測結果，作為 FR-M06 的對照基準 | **P0** | L1 |
| FR-M08 | 獎勵指標定義：計分規則可於管理介面設定，設定內容對全體護理師公開可查 | P1 | L2 |
| FR-M09 | 觀察期模式：獎勵指標可設為「只計算不計分」，用於上線初期驗證資料無系統性偏差 | P1 | L2 |
| FR-M10 | 個人自我檢視：每位護理師可查閱自己的計分明細與原始事件，並提出更正申請 | P1 | L3（限本人） |
| FR-M11 | L3 總開關：個人層級績效資料預設關閉，開啟與關閉均寫入稽核軌跡，且需記錄核准依據 | **P0** | — |
| FR-M12 | 簡報模式：一頁式成效摘要（含基準對照與未達成事項），供會議與訪視使用 | P2 | L1 |

**FR-M04 是本節唯一的 P0 硬性擋下項**：其餘功能給的是建議，護理長可以不採納；但系統不得產出違反勞動法規或醫院規定的班表。

**FR-M07 的時效性警告**：成效基準只能在系統正式啟用前取得。一旦紙本流程消失，就再也無從量測。這一項的實作優先權因此高於它的複雜度所暗示的順序。

**禁止事項（實作時的硬性界線）**：

- 不得建立「護理師×病人臨床結果」的關聯評分
- 不得產生公開的個人排名
- 不得將績效彙整結果回寫至臨床資料表
- 不得以回應速度作為自動派工的依據

### 5.4 對外揭露（0910 新增，對應新增任務一之 4）

| 編號 | 需求描述 | 優先權 |
|---|---|---|
| FR-X01 | 對外報表產生功能：獨立模組，具備自己的權限控制，是資料離開院內的唯一合法管道 | P1 |
| FR-X02 | 對外報表僅輸出聚合值；樣本數少於門檻（預設 5）的分組不呈現；禁止欄位（姓名、病歷號、床號、工作 ID、精確時間戳）於產生時即排除 | **P0**（只要 FR-X01 做，這條就必須同時做） |
| FR-X03 | 對外報表須經指定核准角色確認後方可取出 | P0 |
| FR-X04 | 每一次對外報表的產生與核准均寫入稽核軌跡：誰產生、給誰、包含哪些指標、核准者 | P0 |
| FR-X05 | 對外展示環境：獨立機器、獨立資料庫檔案、資料全為合成，與正式環境無任何連線 | P2 |

**設計說明**：對外揭露真正有說服力的素材是工程證據（架構、治理規範、測試手冊）與可操作的展示環境，這兩者本來就不含病人資料。成效數字（FR-X01～X04）是補充，不是主體。

### 5.5 系統與整合

| 編號 | 需求描述 | 優先權 | 0910 |
|---|---|---|---|
| FR-S01 | **裝置佈建狀態的紀錄與稽核**：每台平板的佈建狀態、Kiosk 設定、序號與金鑰核發、停用與汰換，皆於系統內登記並留下軌跡 | P0 | **0919 改寫**：由「MDM 整合」改為本項。真 MDM 串接由 P0 降為「院方若已有既有方案再接」 |
| FR-S02 | 所有裝置綁定／解除、資料存取、AI 輸出使用情形、L3／L4 查詢、對外報表產生均需寫入稽核軌跡 | P0 | 擴充範圍 |
| FR-S03 | AI 呼叫一律經後端服務轉發，前端不持有任何 API 金鑰 | P0 | 不變 |
| FR-S04 | 病人可識別資訊不得傳送至任何院外服務；去識別化須在寫入資料庫前完成 | P0 | 強化：從「不送外部 LLM」擴大為「不出院內網路」 |
| **FR-S05** | 臨床資料來源介面層（`ClinicalDataSourcePort`）：檔案匯入、院方 API、人工輸入三種實作共用同一介面與同一組內部資料表 | P0 | **新增** |
| **FR-S06** | 模型供應者介面層（`LlmProviderPort`）：模擬、實驗室 API、地端推論三種實作可透過設定切換，不需改程式碼 | P0 | **新增** |
| **FR-S07** | 即時通知一律以院內連線（SSE）提供；**系統不得包含任何院外推播中繼的呼叫** | P0 | **新增（取代原推播需求）** |
| **FR-S08** | 功能開關中心：高風險三項、L3 績效、輪播各層、AI 功能，皆可獨立開關，切換寫入稽核軌跡 | P0 | **新增** |
| **FR-S09** | 備份與還原作業介面：可觸發線上備份、檢視備份歷程，備份動作寫入稽核軌跡 | P1 | **新增（SQLite 單檔風險的對應）** |
| **FR-S10** | **介面呈現不得洩漏內部技術識別**：需求編號（FR-xx）、待確認編號（Q-xx）、迭代編號、資料表與欄位名、列舉常數值、開關識別字、介面層類別名，一律不得出現在任何使用者看得到的畫面文字上 | **P0** | **0919 新增** |
| **FR-S11** | **介面版位與功能可設定**：導覽列版位（主列／下拉選單／關閉）、需手持裝置的功能群組、首波試用要關閉的功能，皆可由具權限者於系統設定調整，切換寫入稽核軌跡 | **P0** | **0919 新增** |
| **FR-S12** | **版本更新不得破壞既有資料**：更新前強制完成一次可還原的備份；資料庫遷移一律前滾、不得於正式環境執行任何會重建資料庫的指令；更新後須可回到上一版 | **P0** | **0919 新增** |

**FR-S06 的過渡期限制**：實驗室 API 為院外服務，因此在切換至地端推論之前，**系統不得將任何真實病人資料送入 AI 流程**，僅得使用合成資料。此限制以程式強制（設定為 `LabApiProvider` 時，帶有真實病人資料的請求應被擋下），不依賴人的自律。

**FR-S10 的判準（0919）**：判準不是「有沒有寫技術名詞」，而是**「這個字對使用者有沒有意義」**。護理師需要知道「這個開關管的是什麼功能」，不需要知道它在程式裡叫什麼；病人需要知道「已處理」，不需要看到 `RESOLVED`。因此：

| 不得出現 | 應改為 |
|---|---|
| 卡片標題寫「功能開關中心（FR-S08）」 | 「功能開關」 |
| 狀態欄直接印出 `IN_PROGRESS`、`PENDING` | 「治療中」、「等待審核」 |
| 提示文字寫「待資訊室確認（Q-09）」 | 「此欄位的格式仍在與資訊室確認中」 |
| 說明寫「人工輸入（ManualEntryAdapter）」 | 「人工輸入」 |
| 開關列印出 `PERF_INDIVIDUAL_L3` | 只留中文名稱與一句說明 |

程式碼註解不在此限——**這條規範的範圍是「使用者看得到的字」，不是「原始碼裡的字」**。需求編號仍應留在註解與文件裡，那是可追溯性的來源。

**FR-S11 的三種版位（0919）**：每個功能在導覽上只會是下列三者之一，切換不需要重新部署。

| 版位 | 意思 |
|---|---|
| **主列** | 出現在護理端上方導覽列，每日必用 |
| **選單** | 收在「更多」下拉選單裡，用得到但不是每天 |
| **關閉** | 不出現在任何入口，且**後端端點一併擋下**，不是只把連結藏起來 |

**FR-S12 的四條硬性規則（0919）**：完整規則見《[部署規範](deployment-spec.md)》與《[資料庫使用規範](database-policy.md)》，此處只列需求本身。

1. 更新作業在備份未成功完成前**不得繼續**；備份必須是可還原的，不是複製出一個檔案就算數
2. 遷移一律前滾：已套用過的遷移檔不得修改，要改就新增一個
3. 正式環境不得執行任何「偵測到差異就重建資料庫」性質的指令
4. 舊版本在新版穩定前不得刪除；資料庫已遷移時，回到舊版的方式是還原備份，不是把遷移倒回去

### 5.6 高風險功能控制（0910 新增）

本節適用於 FR-P03、FR-P05、FR-N06 三項。**八條全部滿足才得實作與啟用，缺一不可。**

| 編號 | 控制要求 | 對應 |
|---|---|---|
| FR-R01 | 三項功能的計算一律由**規則引擎**執行，不得使用語言模型產生判斷結果 | FDE 報告 R-1 |
| FR-R02 | 規則門檻值由臨床端書面提供並版本化儲存；未取得書面依據前，該規則只得對合成資料執行 | R-2 |
| FR-R03 | 每一則輸出可展開完整依據：所引用的歷史資料、套用的規則條目、門檻值、規則版本 | R-3 |
| FR-R04 | 輸出一律標示「僅供參考，非診斷結果」；措辭須為事實陳述（「符合 X 規則：近三次療程第 2 小時後曾回報頭暈」），不得為結論陳述（「病人有低血壓風險」） | R-4 |
| FR-R05 | 輸出不自動生效、不自動觸發任何動作：不得自動調整醫囑、不得自動改變處理優先順序、不得自動通知醫師 | R-5 |
| FR-R06 | 記錄「誰檢視、是否採納、未採納原因」，寫入稽核軌跡 | R-6，對應 FR-N13 |
| FR-R07 | 三項功能各有獨立開關，護理站管理者可即時關閉；關閉後介面完全不出現相關內容 | R-7，對應 FR-S08 |
| FR-R08 | **書面確認閘門**：未取得醫院法務／資訊室對 TFDA 分類的書面確認前，三項功能不得對真實病人啟用。系統應以設定強制此限制，而非僅寫在文件裡 | R-8 |

**法規立場說明**：以上設計取向是讓這三項功能盡可能停留在「提供資訊、由專業人員自行判斷」的範圍，而非「軟體自行做出臨床結論」。這樣的設計能否使其免於醫療器材管理，判定權在主管機關與醫院法務，開發端不做此判斷。判定完成前，功能可開發、可用合成資料驗證，但不對真實病人啟用。

---

## 6. 非功能性需求

| 類別 | 需求 | 0910 |
|---|---|---|
| 效能 | 症狀回報送出至護理端接收延遲需 < 5 秒（正常院內網路） | 不變 |
| 效能（AI） | 即時性質的 AI 功能（SOP 查詢）回應需 < 10 秒；非即時性質（衛教生成、記錄草擬）可走背景佇列，但須有明確進度提示 | **新增。0912 更新：正式主機為 DGX Spark，載入量充裕但記憶體頻寬有限且與其他專案共用，實際可達成值仍待實測（Q-07）** |
| 效能（資料庫） | 統計與報表查詢不得阻擋臨床寫入；長查詢一律走唯讀連線 | **新增（SQLite 單一寫入者的限制）** |
| 可用性 | 病人端療程期間介面需穩定運作 4 小時以上不中斷；護理端多裝置存取一致 | 不變 |
| 可用性（通報） | 因不做推播，**護理站須有常駐總覽螢幕**（FR-N12）。此為部署必要條件，非選配 | **新增** |
| 安全性 | Session Token 限時效、限裝置；所有 API 呼叫需 TLS 加密；資料庫檔案與備份依《資料庫使用規範》第 9 條保護 | 調整 |
| 網路邊界 | **系統不得包含任何主動連向院外的連線**（過渡期的實驗室 API 為唯一例外，且僅得送合成資料） | **新增** |
| 離線韌性 | 衛教與輪播靜態內容可離線瀏覽；症狀回報可離線暫存並於恢復連線後同步；求助功能離線時需明確告知限制 | 擴充至輪播 |
| 多語言 | 衛教與輪播內容、介面至少支援繁體中文；可擴充其他語言 | 擴充至輪播 |
| 可用性（病人端） | 病人多為長者。字級、對比、觸控目標尺寸須符合高齡友善原則；輪播文字尺寸須可從床上距離辨讀 | **新增** |
| **可用性（介面設計基準）** | 兩端的介面須依各自使用族群設計，不得共用同一套版面假設：**病人端**面對躺著、單手、視力與握持受限、可能在打針手不能動的透析病人；**護理端**面對站著、時間被切碎、同時盯多床的護理師。設計準則、族群分析與驗收清單見《[介面設計基準](../notes/uiux-design-baseline.md)》，**日後所有介面改動一律適用**，不是一次性修正 | **0919 新增** |
| **可用性（介面用語）** | 使用者看得到的文字不得出現內部技術識別（FR-S10） | **0919 新增** |
| 可擴充性 | 前後端架構需可延伸支援管理端儀表板，共用同一套 API 層 | 擴充 |
| 可稽核性 | 任何影響臨床或人事的輸出，皆須可回溯到「依據什麼、誰看過、是否採納」 | **新增** |
| 感染管制 | 平板共用需搭配醫院訂定之消毒 SOP。**閒置輪播使平板成為長時間顯示的公共資訊面板，觸碰頻率不會下降**，消毒需求不因此減輕 | 強化 |
| 可移植性 | 系統不得對正式主機的全域環境有任何要求：**不得要求院方預先安裝 Node.js、npm 或任何執行期相依**，執行期隨交付物一起帶進去。**0919 修正**：正式主機改為 x86-64 Windows 工作站，與開發環境同架構；ARM64 的限制取消。**那台主機與其他專案共用**，因此「不假設全域環境」這一條不但保留，還更嚴格（DEP-04），隔離改在安裝包層級做到（DEP-37） | **0912 新增，0919 修正** |
| 正確性（時間） | 所有以時間為基礎的計算（班表、連續工時與班距檢查、排程備份、稽核時間顯示）一律依伺服器當地時區。**正式環境的時區未確認為 Asia/Taipei 時，系統拒絕啟動**（0919：改為 Windows 主機後仍適用，要檢查的由容器環境變數改為主機時區設定）（DEP-15） | **0912 新增，0919 修正** |
| **可維護性（更新）** | 版本更新不得破壞既有資料，且必須可回到上一版（FR-S12） | **0919 新增** |
| **可用性（首波試用）** | 首波僅開放少數護理師與少數病人。**尚未實作的功能一律不提供服務**：介面上不出現入口，後端端點一併擋下，不以「敬請期待」或停用樣式呈現 | **0919 新增** |

---

## 7. 系統架構需求

- 前端採 PWA 架構，病人端搭配 MDM／Kiosk 鎖定；護理端與管理端為響應式設計，涵蓋工作站／平板／手機，另含常駐總覽螢幕模式
- 後端採 API Gateway／BFF 作為唯一對外入口，內部區分裝置綁定服務、AI 推論代理、規則引擎、臨床資料匯入服務、對外報表服務
- 資料層為單一 SQLite 檔案，置於院內伺服器本機磁碟；即時狀態由後端行程內部維護，不另設快取服務
- **無任何主動連向院外的元件**。原架構圖中的推播中繼與外部 LLM 兩條跨院外連線，於本版全部移除
- 地端 AI 推論服務部署於院內伺服器；過渡期改指向實驗室 API，切換方式為設定變更（FR-S06）
- ~~**0912**：正式環境以容器部署於院內 NVIDIA DGX Spark（ARM64、128GB 統一記憶體、與其他專案共用）~~ → **0919 取代**：正式環境為**院內一台 Windows 工作站**（Intel i7-14700、64GB 記憶體、2TB SSD ×2、x86-64，**與其他專案共用**，院方確保本系統資源足夠），以**離線安裝包**部署，資料庫檔案置於該機本機磁碟。容器降為備案，詳見《[部署規範](deployment-spec.md)》
- **0912 新增，0919 強化**：地端推論服務**不由本系統部署或啟動**。它在**另一台機器**（DGX Spark）上，對本系統而言只是一個可設定的 HTTP 端點；本系統不需要 GPU 存取權限，也不需要與它同機（DEP-16）
- **0919 新增**：病人端平板**不經 MDM 管理**。PWA 由一支自包 WebView 外殼 App 載入，Kiosk 靠 Android 內建的螢幕固定，院內自簽 CA 打包進該 App。系統端不含任何裝置管理通道，只保留佈建狀態的紀錄與逸出偵測（FR-S01）

**架構圖現況**：《[架構圖](architecture-diagrams.md)》已於 2026-09-11 依本章重繪，推播中繼、外部 LLM 與 PostgreSQL 三處已拿掉；0919 依本節重繪第二次，應用伺服器與 AI 推論端點分成兩台機器，MDM 那格拿掉改為自包外殼與螢幕固定。圖與本章文字若有出入，仍**以本章文字為準**。

---

## 8. 資料治理與法規遵循需求

- FR-P03、FR-P05、FR-N06 三項於上線前須與醫院法務／資訊室確認 TFDA 醫療器材軟體認證範疇，並取得書面確認（FR-R08）
- AI 與規則引擎的輸出於介面上須明確標示「僅供決策輔助，非診斷結果」（FR-R04）
- 所有 AI 呼叫、規則執行、資料存取、裝置綁定／解除、L3／L4 查詢、對外報表產生，須留存可稽核之操作紀錄，留存期限依《資料庫使用規範》第 8.2 條
- 病人可識別資訊不得以任何形式離開醫院網路；去識別化須於寫入資料庫前完成
- 護理師績效資料屬目的外利用，適用《資料庫使用規範》第 11 條的分級與啟用限制，個人層級預設關閉
- 任何要離開院內的統計數字，須經對外報表模組產生並取得核准（FR-X01～X04）

---

## 9. 裝置管理需求（0919 全節改寫：免 MDM）

**改寫原因**：Android Enterprise 有三處必須連往院外（首次佈建連 Google、後端設定政策要呼叫 `googleapis.com`、遠端鎖定命令靠 FCM 送達），只要採用它，第 10 章假設 5「所有資料不得離開醫院網路」當場不成立。選型過程、三種容器的比較與守不住什麼，見《[平板管控：免 MDM 的全院內方案](../notes/mdm-offline-plan.md)》。

- 15 台平板以**自包 WebView 外殼 App** 載入病人端 PWA；該 App 不含業務邏輯，只負責全螢幕、憑證信任、行為限制與前景回報
- **Kiosk 鎖定使用 Android 內建的螢幕固定**（設定 → 安全性 → 螢幕固定，並勾選「解除固定前要求 PIN」），不需要 MDM、不需要 Google 帳號、不需要任何對外連線
- 院內自簽 CA 的根憑證**打包進該 App 的資源檔**，以網路安全設定宣告為院內網域的信任錨點；不採用「手動安裝到裝置信任存放區」的做法
- 裝置身分（序號與金鑰）存在 App 私有儲存，取代一次性佈建網址；**佈建為每台一次的人工作業**，步驟見平板管控方案 6.1
- 「遠端鎖定」以應用層等效做法取代：解除綁定即回到中性等待畫面並清除本地暫存（FR-P01、SRS 4.3 步驟 8）
- **逸出偵測取代逸出防止**：平板定期回報前景狀態，離開前景立即回報，護理端總覽直接顯示哪一台跳出了 Kiosk。守不住「有人解開而沒人發現」這件事，改成「解開了一定被看見」
- 螢幕恆亮由外殼 App 的常亮旗標提供，比網頁的 Wake Lock 可靠；閒置輪播使螢幕長時間亮著，對電池與螢幕壽命的影響仍須追蹤（15 台的汰換成本）
- 常駐總覽螢幕不在本節範圍：它是護理端裝置，不裝這支 App
- **本階段仍不實作任何真 MDM 串接**。院方若已有既有 MDM 方案，再以新增一個實作的方式接上，介面不變（FR-S01）

---

## 10. 假設與限制

| # | 假設或限制 | 0910 |
|---|---|---|
| 1 | 醫院已同意去識別化資料可用於院內 AI 推論 | 調整（原為「可用於模型訓練」） |
| 2 | 透析中心具備穩定之院內 Wi-Fi 網路覆蓋 | 不變 |
| 3 | 不假設病人具備操作智慧型裝置之熟練度，介面需保持極簡 | 不變 |
| 4 | 機台參數整合、藥局系統雙向寫入列為未來擴充，本版不實作 | 不變 |
| 5 | **所有資料不得離開醫院網路**，因此不實作推播通知 | **新增** |
| 6 | **AI 推論於院內 NVIDIA DGX Spark 執行**（0912 確認：ARM64、128GB 統一記憶體、GB10 Grace Blackwell）。載入量充裕，但記憶體頻寬約 273 GB/s 且**與其他專案共用**，可用額度與實際回應速度尚未定案（Q-07）。**0919 修正：那台機器不是本系統的應用主機，只是一個 API 端點** | **0912 改寫，0919 修正** |
| 7 | **實驗室 API 尚未取得**；在取得並切換至地端推論前，AI 功能僅得對合成資料執行 | **新增** |
| 8 | ~~輪播第三層的臨床數值來源（API 或 Excel）尚未確定~~ → **0919：確定為院方 API。** 欄位定義、認證方式與抓取頻率仍待資訊室給細節（Q-09），但形態已定 | **0919 改寫** |
| 9 | **三項高風險功能的規則門檻值尚未由臨床端提供**，在此之前只能對合成資料執行 | **新增** |
| 10 | **正式環境資料庫為 SQLite**，不接醫院既有資料庫；併發寫入受單一寫入者限制 | **新增** |
| 11 | ~~正式環境以容器部署，交付物為離線映像檔~~ → **0919 改寫：正式環境為院內一台 Windows 工作站**（Intel i7-14700、64GB、2TB SSD ×2、x86-64，與其他專案共用），以**離線安裝包**部署；院內主機不需要對外連線即可安裝。開發環境維持本機直跑。容器降為備案 | **0912 新增，0919 改寫** |
| 12 | **正式主機為桌上型機器，無備援電源、無磁碟陣列、記憶體不具 ECC**。備份目的地因此必須是院內另一台機器，不接受同機備份。**0919：兩顆 SSD 屬同一台機器，第二顆可作為備份的第一落點，但不得是唯一落點** | **0912 新增，0919 補充** |
| 13 | **首波僅開放少數護理師與少數病人試用**，不是全單位上線。因此：尖峰併發遠低於設計值、成效基準量測（Q-01）順延至下週、未實作功能一律不提供服務 | **0919 新增** |
| 14 | **三項高風險功能（FR-P03／P05／N06）於第一次部署時尚未實作**，介面上完全不出現，後端端點一併擋下。它們與管理儀表板（FR-M03／M05／M06／M08～M12）、對外揭露（FR-X）同列下週任務 | **0919 新增** |
| 15 | **護理師手上沒有平板或手機**，護理端只在護理站的一兩台電腦上開啟。所有需手持裝置才能在正確時點操作的功能一律關閉，可於系統設定開啟（FR-S11） | **0919 新增** |
| 16 | **病人端平板不經 MDM 管理**，改走自包 WebView 外殼加螢幕固定；此路線守不住「有人解開螢幕固定」，改以逸出偵測讓它被看見（第 9 章） | **0919 新增** |

---

## 11. 未來擴充項

- 護理站大螢幕儀表板的進階視覺化
- 與 HIS／EMR 既有系統之唯讀整合
- 透析機台資料直接整合（需評估藍牙／序列埠通訊，屆時可能需升級為混合式架構）
- 血管通路影像的 AI 初篩（本版排除，SaMD 分類風險高於文字規則）
- 若醫院日後要求資料併入院內資料庫，依《資料庫使用規範》第 6 條保留的相容性約束進行遷移

---

## 附錄 A：0909 → 0910 需求異動對照

| 編號 | 0909 狀態 | 0910 狀態 |
|---|---|---|
| FR-P01～P02、P04、P06～P08 | 已定義 | 維持，部分改由地端模型生成 |
| FR-P03、FR-P05 | 高風險，實作規格書排除 | **納入**，受 FR-R01～R08 約束 |
| FR-P09～P13 | — | **新增**（閒置輪播） |
| FR-N01～N05、N07～N09 | 已定義 | 維持 |
| FR-N06 | 高風險，實作規格書排除 | **納入**，受 FR-R01～R08 約束 |
| FR-N10～N13 | — | **新增**（求助結果登記、再發標示、常駐總覽螢幕、高風險輸出檢視登記） |
| FR-M01～M12 | — | **新增**（排班、調動、成效、獎勵） |
| FR-X01～X05 | — | **新增**（對外揭露與釋出審核） |
| FR-S01～S04 | 已定義 | 維持，S04 範圍擴大 |
| FR-S05～S09 | — | **新增**（資料來源介面層、模型介面層、院內即時通知、功能開關、備份介面） |
| FR-S10～S12 | — | **0919 新增**（介面不得洩漏內部技術識別、版位與功能可設定、更新不得破壞既有資料） |
| FR-R01～R08 | — | **新增**（高風險功能控制措施） |
| 推播通知需求 | 系統元件之一 | **移除** |

---

## 附錄 B：十大工作區塊分類

見《[FDE 數位化／數位優化評估報告](fde-assessment.md)》第三章。本 SRS 第 5 章的功能需求即依該文件之任務拆解而來；第 5.3 節另對應該文件新增的第十一區塊（單位人力與績效管理）。

---

## 附錄 C：院方授權採用的預設選項與題庫（0919 新增）

> **2026-09-19 已於迭代 9 全數實作。** 本附錄的每一份內容都已寫進資料表，
> 並可在護理端「系統管理 → 內容與選項」調整；四條護欄由後端與前端各擋一次。
> 唯一與本文附錄不同的是**版本號從 2 起算**：第 1 版留給迭代 9 之前那一版題目，
> 只在既有環境（已經有人填過舊版問卷）才會被寫入，全新環境不會有它。
> 操作步驟見《[完整手動測試手冊](../testing/iteration-9.md)》迭代 9 分冊 `s13`–`s34`。

### C.0 這份附錄的法律地位與維護方式

**院方（護理部）於 2026-09-19 授權：先採用以下由 AI 產生的預設內容上線，並要求全部可在護理端「系統管理 → 內容與選項」調整。**

這改變的不是內容，是內容的身分：

| | 0919 之前 | 0919 之後 |
|---|---|---|
| 這些選項是什麼 | 開發端猜的暫代值，等護理部回覆 | **院方授權採用的現行版** |
| 誰可以改 | 只有改程式才能改 | 護理長與護理站管理者，線上改，即時生效 |
| 對應的待確認事項 | Q-13、Q-18、Q-23、Q-24 卡著 | 四項改為已回覆，改以「上線後依實際使用回頭檢討」追蹤 |

**維護規則（四條，寫在這裡是因為它們決定資料能不能用）**：

1. **識別碼不可改，顯示文字可改。** 每一個選項都有一個固定識別碼；護理部要把「現場處置」改成「床邊處置」，改的是顯示文字，識別碼不動。**識別碼一改，這個選項之前累積的所有紀錄就與之後的對不起來。**
2. **停用不刪除。** 不再使用的選項標為停用，不出現在新的選擇畫面，但既有紀錄仍查得到它的文字。
3. **每一次增刪改寫入稽核軌跡**，記錄誰改的、改了什麼、什麼時候。
4. **新增選項要指定它屬於哪一組**，不得憑空多出一個層級。

### C.1 設計原則：為什麼選項要長成這樣

院方要的不只是「畫面上有東西可以選」。這些選項最終要支撐兩件事：

| 數位優化的方向 | 這些選項要提供什麼 |
|---|---|
| **對個別病人的客製化** | 同一位病人的歷次紀錄要能直接相比。今天抽筋比上次嚴重、這個月第三次因為同一件事求助、上次衛教測驗錯在鉀離子——**這些都只有在選項固定且可比較時才算得出來** |
| **用大量資料優化全體病人的透析** | 要能當成模型的輸入特徵與標註。**求助類別（問題）→ 處理方式（做了什麼）→ 處理結果（有沒有用）→ 有沒有再發**，這四段串起來就是一筆完整的標註資料 |

因此五條設計原則：

| # | 原則 | 不這樣做會怎樣 |
|---|---|---|
| C-1 | **每個選項有固定識別碼，顯示文字與識別碼分離** | 護理部改一次字，歷史資料就斷成兩段，趨勢圖從那天起失真 |
| C-2 | **窮盡且互斥，一律保留「其他」並要求填自由文字** | 沒有「其他」，人會硬塞進最接近的那一格，整批資料被汙染而且看不出來 |
| C-3 | **同一份清單裡粒度一致**，不混「症狀」與「情境」 | 混了之後統計出來的百分比沒有意義——分母不是同一件事 |
| C-4 | **四段可串成因果鏈**，共用同一個事件編號 | 只知道發生過什麼，不知道做了有沒有用，訓練不出任何東西 |
| C-5 | **情境欄位由系統自動帶入，不問人** | 療程第幾小時、超過濾進度、班別、距上次透析幾小時——問了會增加操作步驟（前提四），而系統本來就知道 |

**「其他」的使用率本身就是指標**：某一組選項的「其他」超過設定比例（預設 15%），系統要在管理介面標示「這份清單可能不夠用」，提醒回頭檢討。**清單品質不靠一次設計對，靠上線後看得見它哪裡不對。**

### C.2 求助類別（FR-P06，病人端）

**選取依據**：血液透析治療中最常見的併發症，依臨床文獻的常見程度排序，前三項（透析中低血壓、肌肉痙攣、噁心嘔吐）就涵蓋大多數療程中事件。**病人看到的是白話症狀，不是診斷名稱**——病人不會說「我低血壓」，會說「我頭暈想吐」。

臨床組（依常見程度排序）：

| 識別碼 | 病人看到的字 | 對應的常見併發症 | 分派表預設急迫度 |
|---|---|---|---|
| `SX_DIZZY` | 頭暈、冒冷汗、快昏倒 | 透析中低血壓——**最常見** | 緊急 |
| `SX_CRAMP` | 抽筋 | 肌肉痙攣 | 急 |
| `SX_NAUSEA` | 想吐、噁心 | 噁心嘔吐 | 急 |
| `SX_CHEST` | 胸悶、胸痛、很喘 | 胸痛／呼吸困難 | 緊急 |
| `SX_HEADACHE` | 頭痛 | 頭痛／失衡症候群 | 一般 |
| `SX_ITCH` | 皮膚很癢 | 搔癢 | 一般 |
| `SX_CHILL` | 發冷、發抖、覺得發燒 | 發燒畏寒（可能為感染） | 緊急 |
| `SX_ACCESS` | 打針的地方流血、腫、痛 | 血管通路問題 | 緊急 |
| `SX_BLEEDING` | 其他地方流血止不住 | 出血 | 緊急 |
| `SX_OTHER_BODY` | 身體其他地方不舒服 | — | 急 |

非臨床組（**0920 起多一項**）：

| 識別碼 | 病人看到的字 | 分派表預設急迫度 |
|---|---|---|
| `SX_MACHINE` | 機器一直叫 | 急 |
| `NC_TOILET` | 我要上廁所 | 一般 |
| `NC_ENV` | 會冷／會熱／燈太亮／要蓋被子 | 一般 |
| `NC_THIRST` | 口渴、肚子餓 | 一般 |
| `NC_OTHER` | 其他，請有人過來一下 | 一般 |

> **`SX_MACHINE` 於迭代 9.1（0920）由臨床組移到非臨床組**，並排在該組第一個。
> 機台警報不是身體症狀，病人照著「身體不舒服」找不會找到它。
> **預設急迫度與分派規則都沒有變**：非臨床＋急仍然分派給護理師，不會掉到護佐那一條。

**三件必須講清楚的事**：

1. **「分派表預設急迫度」是靜態對照表，不是系統的判斷。** 它只決定「這一則先跳到誰的畫面上」，符合 FR-N03 對分級依據的限制（病人自選急迫度＋靜態分派表，不得摻入模型推論）。**病人仍可自行調整急迫度，病人選的優先於預設值。**
2. **這張表不是分診，畫面上不得出現任何疾病名稱。** 右欄的「對應併發症」只存在於本文件與後端對照資料，**不顯示給病人也不顯示給護理師**——顯示了就變成系統在給診斷提示，那是 FR-P03／P05 的範圍，本階段未實作。
3. **臨床組的順序是刻意的**：最常發生且最需要快速反應的排最前面，讓病人在不舒服時第一眼就看到。**不依字母、不依識別碼排序。**

### C.3 處理方式（FR-N10，護理端結案時選）

**設計對齊 C.2**：既然求助類別改成以常見併發症為骨架，處理方式就要涵蓋這些併發症實際會做的處置，否則護理師只能一直選「現場處置」，資料等於沒蒐集到。

| 識別碼 | 顯示文字 | 說明（只給護理端的小字提示） |
|---|---|---|
| `M_OBSERVE` | 觀察並記錄生命徵象 | 量血壓、脈搏，記錄下來 |
| `M_POSITION` | 調整姿勢 | 平躺、抬腿、翻身 |
| `M_UF_ADJUST` | 調整超過濾（依醫囑） | 降速或暫停 |
| `M_MACHINE_SET` | 調整機台設定（依醫囑） | 血流速、透析液溫度或鈉濃度 |
| `M_SALINE` | 給予生理食鹽水回填（依醫囑） | |
| `M_MEDICATION` | 給藥（依醫囑） | |
| `M_LOCAL_CARE` | 局部處置 | 止血、加壓、冰敷、按摩抽筋處 |
| `M_ACCESS_CARE` | 血管通路處置 | 重新固定、換敷料、檢查震顫 |
| `M_NOTIFY_MD` | 通知醫師 | |
| `M_EARLY_END` | 提前結束療程（依醫囑） | |
| `M_SUPPLY` | 提供物品 | 被子、水杯、尿壺 |
| `M_ASSIST_ADL` | 協助如廁或移位 | |
| `M_ENV_ADJUST` | 環境調整 | 空調、燈光、床位角度 |
| `M_REASSURE` | 說明與安撫 | |
| `M_REFERRAL` | 轉介 | 社工、營養師、感染管制 |
| `M_OTHER` | 其他（請說明） | **選了必須填自由文字** |

**「依醫囑」四個字不是客套話。** 帶著它的選項記錄的是「護理師執行了醫師的指示」，不是「系統建議這樣做」。沒有這四個字，同一批資料日後被拿來訓練模型時，會變成「系統學會了在某某情況下要調降超過濾」——那就跨進 SaMD 了。**這是內容設計層面的風險控制，不是措辭潔癖。**

可複選：一次求助常常做了不只一件事。**複選時要記錄順序**，因為「先平躺再回填食鹽水」和「先回填再平躺」是不同的處置路徑。

### C.4 處理結果（FR-N10）

| 識別碼 | 顯示文字 |
|---|---|
| `O_RESOLVED` | 症狀解除，療程照原計畫完成 |
| `O_IMPROVED` | 症狀改善，持續觀察 |
| `O_UNCHANGED` | 症狀沒有改善 |
| `O_ESCALATED` | 已升級處理（醫師到場或接手） |
| `O_ORDER_CHANGED` | 已依醫囑調整本次療程處方 |
| `O_EARLY_END` | 提前結束療程 |
| `O_TRANSFERRED` | 轉送（急診或病房） |
| `O_NONCLINICAL_DONE` | 非臨床需求已滿足 |
| `O_PATIENT_CANCELLED` | 病人自行取消 |
| `O_UNABLE` | 無法處理（請說明） |

單選。**這一欄是整條因果鏈的答案欄**，複選會讓「這個處置有沒有用」變得無法計算。

**後續追蹤事項**（是否需追蹤＝是時才出現，可複選）：

| 識別碼 | 顯示文字 |
|---|---|
| `F_BP_NEXT` | 下次透析前先量血壓 |
| `F_ACCESS_WATCH` | 追蹤血管通路狀況 |
| `F_DRY_WEIGHT` | 建議重新評估乾體重 |
| `F_EDU` | 需加強衛教 |
| `F_INFECTION` | 通報感染管制 |
| `F_MD_ROUND` | 請醫師下次巡診時看 |
| `F_OTHER` | 其他（請說明） |

### C.5 透析前問卷（FR-P02）

**重新設計的原因有兩個**：原本 10 題全部必答，對躺著、手不能動的長者太長；而且題目全部平行排列，看不出哪幾題是每次都要有的基準。

**新結構：5 題必答（每次都問）＋條件追問（答「有」才展開）＋自動帶入的情境欄位（不問人）。**

必答 5 題——**這 5 題的存在理由是「每次都有同一組欄位，才畫得出這位病人自己的趨勢」**：

| 識別碼 | 病人看到的字 | 白話提示 | 答案尺規 |
|---|---|---|---|
| `Q_FLUID` | 跟上次洗完比，有沒有比較腫 | 腳踝、小腿、眼皮 | 沒有／有一點／腫很多 |
| `Q_DYSPNEA` | 會不會喘 | 走路、平躺、或休息的時候 | 沒有／輕微／中等／嚴重 |
| `Q_DIZZY` | 會不會頭暈 | 站起來的時候或平常 | 沒有／輕微／中等／嚴重 |
| `Q_CRAMP` | 上次洗完到現在有沒有抽筋 | | 沒有／偶爾／常常 |
| `Q_ACCESS` | 打針的地方有沒有狀況 | 瘻管或導管 | 沒有／有 |

條件追問（只在對應必答題答「有」時出現）：

| 觸發 | 識別碼 | 追問內容 |
|---|---|---|
| `Q_ACCESS` = 有 | `Q_ACCESS_DETAIL` | 是哪一種：流血止不住／紅腫熱痛／摸不到震顫／其他（可複選） |
| `Q_DYSPNEA` ≥ 中等 | `Q_ORTHOPNEA` | 晚上睡覺要不要墊高枕頭才睡得著 |
| `Q_CRAMP` ≠ 沒有 | `Q_CRAMP_WHEN` | 抽筋發生在：洗腎中／洗完回家後／半夜 |

每次都問但放在第二頁的 4 題（**分頁是為了第一頁能一眼看完，不是為了少問**）：

| 識別碼 | 病人看到的字 | 答案尺規 |
|---|---|---|
| `Q_FEVER` | 這幾天有沒有發燒、發冷 | 沒有／有 |
| `Q_CHEST` | 有沒有胸悶或胸痛 | 沒有／輕微／中等／嚴重 |
| `Q_ITCH` | 皮膚會不會癢 | 沒有／輕微／中等／嚴重 |
| `Q_EVENT` | 上次洗完到現在，有沒有去急診、住院，或跌倒 | 沒有／有（有→追問哪一種） |

**`Q_EVENT` 是本次新增的最有價值的一題**：透析間期的事件是護理師本來就會口頭問的，但問到的答案留在口頭上。把它結構化，才接得起「上次發生什麼 → 這次的狀況」這條線。

自動帶入、不問病人的情境欄位：

| 欄位 | 從哪來 |
|---|---|
| 距上次透析幾小時 | 前一次療程紀錄 |
| 本次班別 | 排班 |
| 前次療程是否提前結束 | 前一次的求助結案紀錄 |
| 前次透析後回饋分數 | 回饋問卷 |

**題目版本要編號。** 題庫一改就換一個版本號，每一份作答記錄它當時的版本；沒有版本號，改過題目之後的趨勢圖就是把兩種不同的問題畫在同一條線上。

### C.6 透析後衛教題庫（FR-P07）

**七個主題，對應透析族群最常見的自我照護失誤**，每個主題 5 題，共 35 題。每次測驗抽 5 題。

| 識別碼 | 主題 | 為什麼是它 |
|---|---|---|
| `EDU_FLUID` | 水分與體重控制 | 透析間期體重增加過多是最常見也最直接影響療程的問題 |
| `EDU_K` | 鉀離子（高鉀食物） | 高血鉀可致命，且來源多半是吃進去的 |
| `EDU_P` | 磷（高磷食物與磷結合劑） | 長期影響骨骼與血管，病人最容易忽略 |
| `EDU_NA` | 鹽分與口渴的關係 | 病人常以為「少喝水」就好，不知道問題在鹽 |
| `EDU_ACCESS` | 血管通路照護 | 通路失效等於治療中斷，而多數傷害是可預防的 |
| `EDU_MED` | 用藥與返診 | 漏藥、自行停藥、忘記回診 |
| `EDU_WARN` | 什麼情況要馬上找人 | 把「何時該求助」本身當成一個知識點來教 |

**每一題都要標上知識點識別碼**，答錯即累積在那位病人的該知識點上。

**抽題規則（這就是「個人化」的具體做法，不是抽籤）** —— 依序取滿 5 題：

| 順位 | 抽什麼 | 理由 |
|---|---|---|
| 1 | 這位病人**上次答錯**的知識點 | 沒學會的要再問 |
| 2 | **本次療程實際發生過的事**對應的主題 | 今天抽筋又體重增加多 → 抽水分控制題。當天發生的事，當天講最有效 |
| 3 | **最久沒被問到**的主題 | 避免永遠只問同幾題 |
| 4 | 隨機補足 | |

**知識點與題庫的維護**：主題可增刪，題目可改寫，但**知識點識別碼與題目識別碼不可重用**——一個識別碼一輩子只對應一個知識點，否則「這位病人在鉀離子上錯了三次」這句話會變成假的。

### C.7 回饋題目（FR-P08）

**設計取捨**：每次都問太多題，病人會開始亂按，資料反而變髒。因此分成「每次問的 4 題」與「每月加問的 2 題」。

每次療程後（目標一分鐘內答完）：

| 識別碼 | 題目 | 尺規 |
|---|---|---|
| `FB_COMFORT` | 今天洗腎的過程，身體舒不舒服 | 五格表情，很不舒服→很舒服 |
| `FB_MOOD` | 今天的心情 | 五格表情 |
| `FB_CARE` | 有沒有覺得被好好照顧 | 五格表情 |
| `FB_NOTE` | 有沒有什麼想讓我們知道的 | 自由文字，**選填** |

每月加問（同一位病人一個月內只出現一次）：

| 識別碼 | 題目 | 尺規 |
|---|---|---|
| `FB_BURDEN` | 最近會不會覺得洗腎很累、不太想來 | 五格 |
| `FB_SLEEP` | 最近睡得好不好、吃得下嗎 | 五格 |

**「趨勢偏離」怎麼判定（這一條要寫死，因為它最容易滑進臨床判斷）**：

- 比較的是**這位病人自己**近六次的移動中位數，不是跟其他病人比
- 連續兩次低於自己的中位數達設定幅度（預設 2 分）才標示
- **判定由固定算式完成，不經語言模型**
- 輸出是事實陳述：「近兩次的心情分數比自己過去六次的中位數低 2 分以上」。**不得寫成「疑似憂鬱」「建議轉介」之類的結論**
- **不自動觸發任何動作**，不自動通知社工，只在護理端顯示
- 門檻（六次、2 分、連續兩次）全部可在系統設定調整

**這一條的界線**：上面的算式是「把病人自己說的分數拿來相減」，屬於原樣呈現；一旦要把它變成主動警示、或摻入其他臨床資料一起判斷，就進入 5.6 節 FR-R 的範圍，必須先滿足那八條。**本階段不跨過這條線。**

### C.8 這些內容如何被調整

全部集中在護理端「系統管理 → 內容與選項」底下，分成五塊，對應 C.2～C.7：

| 區塊 | 可做的事 |
|---|---|
| 求助類別 | 增刪改、調整順序、調整分派表預設急迫度、停用 |
| 處理方式與處理結果 | 增刪改、調整順序、停用；「其他」不可停用 |
| 透析前問卷 | 改題目文字與提示、調整必答／第二頁、增刪題目、**改動即產生新版本號** |
| 衛教題庫 | 增刪改題目與選項、調整主題、設定抽題規則的權重 |
| 回饋題目 | 改題目文字、調整每月加問的間隔、調整趨勢偏離的三個門檻 |

**四條護欄**（介面上要擋住，不是寫在說明裡）：

1. 識別碼欄位在建立後不可編輯
2. 停用是唯一的移除方式，沒有刪除按鈕
3. 每一組的「其他」不可停用、不可刪除
4. 問卷與題庫改動時，畫面要明白告訴操作者「這會產生新的版本，之前的作答仍以舊版本留存」

**0919 迭代 9 實作方式**：四條都是**後端擋一次、前端擋一次**——只藏前端等於沒擋。
前端那一次的做法是「按鈕不出現」，不是「按了才說不行」：
識別碼那一格在編輯時是純文字而非輸入框（護欄 1）、整頁沒有任何刪除按鈕（護欄 2）、
「其他」那一列沒有停用按鈕（護欄 3）、版本提示寫在按下去之前（護欄 4）。

**「其他」的使用率**也做進同一個畫面：超過設定比例（預設 15%，可調）時，
上方會出現一條提醒「這份清單可能不夠用」。剛部署時資料量不足，這一條不會出現。

---

## 版本歷程

| 定版 | 日期 | 異動 |
|---|---|---|
| [0916](https://94sh09sh19sh.github.io/hd-docs/0916/requirements/srs/) | 2026-09-16 | 改版為 v2.0：三項高風險功能由排除改為有條件納入（受八條控制措施約束），推播整條移除改為院內 SSE，AI 改由院內伺服器推論，資料庫改為 SQLite；部署相關敘述改指《部署規範》 |
| [0909](https://94sh09sh19sh.github.io/hd-docs/0909/requirements/srs/) | 2026-09-09 | 首次定版 |

[← 回進度首頁](../index.md)
