# 部署演練紀錄

版本：v0.1　文件狀態：**進行中（部署當天要回來填）**　適用讀者：FDE 工程團隊、醫院資訊室

---

> **0926：交付路線整條換掉。** 院方要求容器＋`git clone`、不准用隨身碟，離線安裝包撤除（《部署規範》v3.0）。
> 第 1～3 節（路線 A、路線 B、乾淨機器）與第 4、5 節裡安裝包的做法**保留為歷史紀錄**，不再是要做的事；
> DEP-25～28 已作廢。**現行的演練結果看第 8 節**，步驟看《[部署手冊](../deployment/index.md)》第十一冊。

## 0. 這份文件是什麼

《[部署規範](../requirements/deployment-spec.md)》DEP-25 要求：**路線 A 的每一次失敗都要記錄下來——
在哪一步、錯誤訊息的原文、花了多久。紀錄本身就是交付物。**

它有兩個用途：向資訊室說明「為什麼不能用一般的方式部署」時，它比任何說明都有效；
以及日後有人提議「直接在主機上更新就好」時，它是現成的答案。

本文件同時收 DEP-26～28（路線 B）、DEP-19／DEP-32（更新與回退）與 DEP-22（乾淨機器）的演練結果。

**要知道每一格怎麼填出來，看《[部署手冊](../deployment/index.md)》**（0921 新增）：
這一份收的是**結果**，那一份寫的是**動作的順序與每一步會怎麼壞**。
現場的對應關係是——[第二冊](../deployment/02-dry-run.md) 填 §3.2、
[第四冊](../deployment/04-onsite.md) 填 §1.3 與 §2.3、
[第三冊](../deployment/03-tablet-shell.md) 填 §6.1。

**每一節分成兩塊**：

| 塊 | 意思 |
|---|---|
| **已完成（開發端）** | 迭代 10 在開發機上實際跑過，輸出照抄 |
| **部署當天要填** | 只有在院內那台主機上才做得出來的，先把欄位列好 |

**不要把開發端那一塊當成驗收通過。** 開發機有網路、有 Node.js、有一份完整的套件目錄，
院內那台三樣都沒有——而那正是整件事的重點。

---

## 1. 路線 A：在正式主機上 `git clone` 並安裝（預期失敗）

**目的不是成功，是取得證據。**

### 1.1 已完成（開發端）：把每一步的失敗重現一次

正式主機還沒拿到，因此在開發機上把「沒有對外連線」這個條件做出來再跑一次，
取得每一步的錯誤原文。**這不能取代現場那一次**（錯誤訊息會因為院內的代理伺服器設定而不同），
但它先回答了「會失敗在哪裡」。

| 步驟 | 重現方式 | 結果 |
|---|---|---|
| `git clone` | — | 未在開發端重現：開發機連得到程式碼來源，重現不出院內那台的情形。**現場照做並記錄** |
| `npm install` | 把套件來源指向一個不存在的位址（`--registry=http://127.0.0.1:9`），並使用空的快取目錄 | **失敗，耗時 71 秒** |
| 套用資料庫遷移 | 見 1.2 | 引擎不在套件裡，會現場下載 |

`npm install` 的錯誤原文（節錄自 npm 的除錯紀錄）：

```
25 error code ECONNREFUSED
26 error syscall connect
27 error errno ECONNREFUSED
28 error FetchError: request to http://127.0.0.1:9/@nestjs%2fcore failed,
   reason: connect ECONNREFUSED 127.0.0.1:9
```

**要看的不是那串錯誤碼，是那 71 秒**：它連「第一個套件的中繼資料」都還沒拿到。
這個專案的相依有數百個，現場不會有人在那裡等它一個一個逾時。

### 1.2 套用遷移：一個裝好了也還是不能用的相依

Prisma 的執行引擎**不在 npm 套件裡**，是安裝套件時由安裝後腳本另外下載的（約 40MB）。
因此即使前兩步奇蹟般成功，這一步仍然會倒在

```
request to https://binaries.prisma.sh/... failed, reason: getaddrinfo ENOTFOUND binaries.prisma.sh
```

這一條在迭代 7 就撞過一次，處置寫在 DEP-30。**安裝包自帶引擎**，路線 B 不會遇到它。

### 1.3 部署當天要填

| 欄位 | 值 |
|---|---|
| 日期／時間 |  |
| 執行者 |  |
| 主機 |  |
| `git clone` 的錯誤原文 |  |
| 耗時 |  |
| `npm install` 的錯誤原文 |  |
| 耗時 |  |
| 有沒有人提議「開一下網路就好」、怎麼回應的 |  |
| **若 `git clone` 成功**：來源（直連／代理／院內鏡像）、用了誰的憑證、連線是否常態、選了哪一條出路（[第七冊](../deployment/07-git-clone.md) G-02～G-06） |  |

> **路線 A 不得為了讓它成功而在正式主機上開對外連線。**
> 若真的開了，零院外連線這個前提就在第一天被自己破壞。

---

## 2. 路線 B：隨身碟帶入離線安裝包（正式路線）

```
開發端                                    院內
建置安裝包 → 壓縮 → 產生雜湊值 → 防毒掃描 → 隨身碟 → 掃描 → 解壓 → 核對雜湊 → 安裝
```

### 2.1 已完成（開發端）：建置與驗收

```
npm run build
npm run check:offline
npm run package:build -- --zip
npm run package:verify -- --dir dist-package/<安裝包目錄>
```

2026-09-20 的建置結果：

| 項次 | 值 |
|---|---|
| 安裝包 | `hd-tablet-care-0.1.0-<commit>` |
| 檔案數／大小 | 23,588 個檔案，681.4 MB（未壓縮） |
| 自帶執行期 | Node.js v24.16.0（win32-x64） |
| 自帶 Prisma 引擎 | `query_engine-windows.dll.node`、`schema-engine-windows.exe` |
| 雜湊值 | `CHECKSUMS.txt` 逐檔 SHA-256；清單本身再取一次 SHA-256 附在交付單上 |

`npm run package:verify` 的五項：

| 項次 | 結果 |
|---|---|
| 雜湊值核對（23,588 個檔案） | 通過 |
| 包內沒有設定檔、資料庫或備份（DEP-13） | 通過 |
| 以包內引擎套用既有遷移到全新資料庫（DEP-31） | 通過 |
| 以包內執行期啟動三個行程並通過存活檢查 | 通過 |
| 全程沒有往院外的連線（連線哨兵） | 通過 |

**這一次驗收在開發機上做的，只證明安裝包內部是完整的。**
它證明不了 DEP-22——那要一台沒有 Node.js、沒有套件目錄、沒有網路的機器。

### 2.2 建置期間抓到的兩個缺陷

演練的價值在這裡：**兩個缺陷都只有在真的把安裝包跑起來時才會現形**。

| # | 症狀 | 原因 | 處置 |
|---|---|---|---|
| 1 | 後端在安裝包裡起不來，錯誤是「找不到專案根目錄」 | 資料庫路徑檢查會往上找帶 workspaces 的 `package.json`（避免資料庫被收進版控）。**安裝包裡沒有那個東西**，於是一路找到磁碟根目錄然後丟例外 | 改成兩段：先找專案根目錄，找不到就找安裝包根目錄（以 `VERSION.json` 與 `runtime/` 辨識）。兩者要擋的是同一件事——資料庫不得放在「每次更新會整個換掉的那個目錄」裡（DEP-08） |
| 2 | `prisma migrate deploy` 在包內找不到 CLI | 指令外套只認 `apps/api/node_modules` 與 monorepo 根目錄兩個落點，安裝包的版面是 `app/node_modules`（少了一層） | 補上第三個落點 |

兩個都不是設定寫錯，是**開發機的版面與安裝包的版面不一樣**，而所有靜態檢查都只看得到前者。

### 2.3 部署當天要填

| 欄位 | 值 |
|---|---|
| 安裝包版本／commit |  |
| 開發端防毒掃描：工具、時間、結果（DEP-26） |  |
| 院內掃描站：位置、經手人、結果、留存位置 |  |
| 解壓後核對雜湊值（DEP-27）：相符？ |  |
| 隨身碟清除時間與執行者（DEP-28） |  |
| 從解壓到服務起來的總耗時 |  |
| 期間出現的任何對外請求（應為 0） |  |

> **隨身碟是單向的**：只能把安裝包帶進院內，不得帶任何東西出院——不是備份、不是紀錄檔、不是截圖。

---

## 3. 乾淨機器啟動測試（DEP-22）

### 3.1 現況

**未完成。** 需要一台沒有開發環境、沒有全域 Node.js 的 Windows 機器。

要做的事：把安裝包與 `scripts/verify-package.mjs` 一起帶過去，**拔掉網路線**，執行

```
<安裝包>\runtime\node.exe verify-package.mjs --dir <安裝包目錄>
```

五項全綠，並把輸出整段貼進 3.2。連線哨兵會把任何一次往院外的 DNS 查詢或 TCP 連線記下來，
**有一筆就不得交付**。

### 3.2 部署當天要填

| 欄位 | 值 |
|---|---|
| 機器（型號、作業系統版本、是否乾淨安裝） |  |
| 網路：實際斷開的方式 |  |
| `verify-package.mjs` 輸出 |  |
| 服務啟動後的存活檢查回應 |  |
| 主機時區（**在服務執行帳號底下確認**，DEP-15） |  |

---

## 4. 更新演練（DEP-19、規範第 18 條）

### 4.1 已完成（開發端）：完整跑過一次，而且跑出一個缺陷

2026-09-20，以正式流程對一份含真實筆數的開發資料庫副本執行 `npm run db:update`：

| 步驟 | 結果 |
|---|---|
| 1 確認服務已停止 | 沒有服務在回應，可以繼續 |
| 2 開啟更新紀錄 | 前一次成功更新的版本：0.1.0 |
| 3 更新前備份 | `hd-before-update-20260920T170519.db`（4,247,552 bytes），SHA-256 `4dd0a9e6…52860a` |
| 4 驗證備份還原得起來 | 完整性檢查 ok；護理端帳號 129、病人 131、綁定 112、求助 120、稽核紀錄 3,080 |
| 5 套用既有遷移 | 套用了 1 個遷移：`20260921030000_iteration10_kiosk_foreground` |
| 6 寫入更新紀錄 | 更新紀錄與稽核軌跡都已寫入 |

**第一次跑的時候第 5 步倒了**：

```
Error: SQLite database error
database is locked
   0: sql_schema_connector::sql_migration_persistence::initialize
```

原因是更新腳本為了寫更新紀錄，一直開著一條連往正本的連線，而遷移引擎要的是獨占存取——
**更新腳本自己擋住自己**。它倒的位置是「備份做完、驗證通過、正要動 schema」的那一刻，
看起來像資料庫壞了。

這個缺陷只有在**這次更新真的帶著待套用的遷移**時才會現形；沒有 schema 變更的更新一路綠燈。
換句話說，**它會在第一次真正需要它的時候失敗**。處置：在套用遷移之前先把腳本自己的連線放掉。

### 4.2 部署當天要填

| 欄位 | 值 |
|---|---|
| 日期／時間（應為非透析時段，DEP-32） |  |
| 執行者 |  |
| 更新前版本 → 更新後版本 |  |
| 備份檔名與 SHA-256 |  |
| 驗證還原的結果 |  |
| 套用了哪幾個遷移 |  |
| 舊版安裝包目錄改名為 |  |
| 啟動後確認的主線功能（症狀回報、求助、總覽、綁定） |  |
| 更新後第一個班次的值班者 |  |

---

## 5. 回退演練（DEP-19、規範 18.3）

**程式可以退版，資料庫不行退版。** 動過 schema 的版本要回退，是**還原備份**再換回舊版程式，
不是把遷移倒回去。

### 5.1 已完成（開發端）

接續 4.1，把該次更新前的備份還原到另一個檔案：

```
npm run db:restore -- --from <備份檔> --to <還原後的路徑> --sha256 <備份歷程上的雜湊>
```

| 項次 | 結果 |
|---|---|
| 雜湊值 | 與備份歷程上的紀錄相符 |
| 完整性檢查 | ok |
| 筆數 | 護理端帳號 129、病人 131、稽核紀錄 3,080、功能開關 11、備份歷程 49 |

還原後與更新後兩份資料庫的對照：

| | 更新後 | 還原後 |
|---|---|---|
| `devices` 的逸出偵測欄位 | 三欄都在 | 沒有（回到遷移前的樣子） |
| 已套用的遷移數 | 9 | 8 |
| 稽核紀錄 | 3,083 | 3,080 |

差的那三筆是更新流程自己寫的（更新開始、備份已驗證、更新完成）——**資料完整，沒有遺失**。

### 5.2 部署當天要填

| 欄位 | 值 |
|---|---|
| 觸發回退的原因 |  |
| 還原的備份檔名與雜湊 |  |
| 換回的安裝包版本 |  |
| 還原後確認的筆數 |  |
| 回到服務可用的總耗時 |  |

---

## 6. 平板佈建（DEP-36、平板管控方案 6.1）

**首波只佈建實際會用到的兩三台**，跑完一個完整班次沒問題再做其餘。
15 台一次做完，等於把佈建的錯誤一次放大 15 倍。

### 6.1 部署當天要填（每台一列）

| 序號 | 床號 | Android 版本 | 側載完成 | 序號金鑰輸入 | 螢幕固定＋PIN | 護理端清單核對 | 備註 |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |

伺服器端（做一次）：

| 項次 | 值 |
|---|---|
| 自簽 CA 產生時間、指紋 |  |
| CA 私鑰異地備份位置（院內，不出院，DEP-35） |  |
| APK 簽名金鑰建立時間、備份位置 |  |
| 伺服器憑證涵蓋的位址（主體別名） |  |

> CA 與 APK 的簽名金鑰**遺失即無法補救**。APK 金鑰遺失的後果是「再也發不出更新版，
> 只能換套件名重新側載 15 台」。

---

## 7. 部署前置檢查清單

逐條確認的表在《[部署規範](../requirements/deployment-spec.md)》第 11 章（v3.0 起，25 條），**缺一條就不算可以部署**。
確認結果請直接標在那一份上（或影印一份逐條打勾，存進部署紀錄）。

迭代 10 完成後，清單上已經可以**事先備妥**的有：第 1、13、15、17、18、19 條所需的工具與腳本；
其餘各條都要在主機與現場才答得出來。（以上為 v2.0 的條號，v3.0 的對應見第 8.6 節。）

---

## 8. 迭代 11：容器部署在開發機上從零實測（0926）

照《[部署手冊](../deployment/index.md)》第十一冊的指令，在開發機上走完 DEP-22 要求的一輪（外殼 App 與 AI 兩項分屬迭代 12、13，尚未納入）。
每一步的原始輸出存在開發機上（repo 之外的 `drill-logs/2026-09-26-iteration11/`，21 份，最後一份是修正後重跑的 `--live`），
不進 repo：鏡像是公開的，輸出裡有開發機的路徑與帳號名稱。

### 8.1 從零部署（驗收 1）

| 條件 | 本次 |
|---|---|
| 全新 clone | 從 GitHub 重新 clone 到暫存目錄，不是平常的工作目錄。**迭代 11 的改動當時尚未 commit，是以 patch 套上去再打本機 tag `drill-v1`**；正式交付前要以真正的 tag 再走一次 |
| 全新資料卷 | 實測前 `docker volume ls` 為空 |
| 只用 Git 與 Docker | 部署步驟全程只下 `git` 與 `docker`；`JWT_SECRET` 也以 `docker run … node -e` 產生 |

| 步驟 | 結果 |
|---|---|
| W-12 建兩個資料卷 | `hd-tablet-care-data`、`hd-tablet-care-keys` 建立 |
| W-13 建置 | `Image hd-tablet-care:drill-v1 Built` |
| W-14 套用遷移 | `SQLite database hd.db created at file:/data/hd.db`，9 個遷移全部套用；**全程沒有對外下載引擎** |
| W-15 啟動 | `api`、`nurse-web`、`patient-web` 三個 `Up`，`ai-gateway`、`shell-builder` 沒有被帶起來 |
| W-16 確認 | health 正常；`synchronous=FULL`、`（正式環境設定）`、`伺服器時區：Asia/Taipei`，**沒有時區警告**（容器裡的 TZ 就是它該看的時區）；病人端產物裡寫的是設定的後端位址；初始帳號建立、首次登入強制改密碼 |
| W-17 還原到測試檔 | SHA-256 相符、完整性檢查 ok，測試檔刪除 |

### 8.2 `verify:iteration11 --live`（驗收 2、3、4、6）

| 驗收 | 結果 |
|---|---|
| 4 repo 裡找不到安裝包；`check:all` 全綠 | ✓（六支全綠） |
| 3 繫結掛載只有兩個 | ✓ 靜態檢查＋以 `docker compose config` 解析後再查：只有 `/backups` 與 `/config` |
| 6 正式環境設定＋`lab` | ✓ 後端結束狀態 1，訊息「正式環境設定下不得使用 LLM_PROVIDER=lab…（《部署規範》DEP-40）」 |
| 2 `down -v` 之後資料庫仍在 | ✓ 資料卷與 `hd.db` 都在，重新 `up -d` 後 health 正常 |

### 8.3 更新演練（驗收 5 前半）

測試 tag `drill-v2` 只存在演練用的 clone：版本標記改成 `0.1.1-drill`，外加一個只建一張空表的遷移，讓這次更新真的帶著遷移。

| 步驟 | 結果 |
|---|---|
| 建置新版 | `hd-tablet-care:drill-v2` 建好，`drill-v1` 保留 |
| 停 `api` → `db-update.mjs` | 確認服務已停 → 更新前備份 `hd-before-update-20260926T140246.db` → 驗證還原（完整性 ok）→ 套用 1 個遷移 → 寫更新紀錄 |
| 啟動 | 版本標記 `0.1.1-drill`；更新前建立的測試平板還在 |
| 服務開著時再跑一次 `db-update.mjs` | **擋下**：「後端服務還在執行…請先停止服務再執行」。這一條在實測前是壞的，見 8.5 |

### 8.4 回退演練（驗收 5 後半）

| 步驟 | 結果 |
|---|---|
| checkout `drill-v1`，`HD_IMAGE_TAG` 改回 | 不必重建，舊映像檔還在 |
| 停 `api`，把現有 `hd.db` 移到資料卷裡的另一個目錄 | 完成 |
| 還原更新前那份備份到 `/data/hd.db` | SHA-256 相符、完整性 ok |
| 啟動 | 三個服務都是 `drill-v1`；版本標記 `0.1.0`；演練遷移建的那張表不在了、遷移紀錄回到 9 筆；更新前建立的測試平板在，**更新後建立的那一台不在**——這是規範 18.3 預期的代價，不是缺陷 |

### 8.5 實測抓到的問題

| 問題 | 為什麼靜態檢查看不到 | 處理 |
|---|---|---|
| `db-update.mjs` 判斷「服務是否還開著」問的是 `127.0.0.1` | 在一次性容器裡，`127.0.0.1` 是它自己，永遠「沒有服務在回應」；**只有真的在容器裡跑才會這樣** | compose 設 `UPDATE_PROBE_URL=http://api:3000/api/health`；8.3 最後一列驗過 |
| 還原腳本在映像檔裡跑不起來 | 它以 ts-node 執行、會讀 `src/`，映像檔原本沒帶 | 映像檔帶入 `apps/api/src` 與 `tsconfig.json`；8.4 驗過 |
| 在 Git Bash 下照抄，還原回報「找不到備份檔」 | Git Bash 把 `/backups/…` 參數轉成 Windows 路徑，與程式無關 | 手冊註明一律用 PowerShell（第十一冊第 7 節） |

### 8.6 還沒做的

| 事項 | 什麼時候 |
|---|---|
| 以正式的 git tag（不是 patch）再走一次 | 迭代 11 的改動 commit 並打 tag 之後 |
| 開發手機從下載頁裝外殼、釘住、護理端看得到（DEP-22 第 2～5 項） | 迭代 12：APK、下載頁與 HTTPS 已在開發機驗過（第 9 節），**手機本身待測** |
| AI 經 SSH 通道接實驗室閘道（DEP-22 第 7 項前半；後半「正式環境設定拒絕 `lab`」已在 8.2 驗過） | 迭代 13 |
| 重新開機後服務自己回來（DEP-21 第 4 項） | 開發機的 Docker Desktop 隨登入啟動，驗不出院內「沒人登入」的情形，要在院內那台驗（Q-27） |
| 部署前置檢查清單 v3.0 的 25 條 | 第 1、15、16、17、19 條的工具與實測已備妥；其餘要在主機與現場答 |

---

## 9. 迭代 12：外殼 APK 在開發機上建置（0927）

照《[部署手冊](../deployment/index.md)》第十二冊，在開發機上走 Y-03～Y-07，再跑 `verify:iteration12 --live`。
原始輸出存在開發機上（repo 之外的 `drill-i12/logs/`，21 份），理由同第 8 節：輸出裡有開發機的路徑與帳號名稱。

| 條件 | 本次 |
|---|---|
| 程式碼 | **本系統是工作目錄，不是全新 clone**：迭代 12 的改動當時尚未 commit。外殼是本機 clone 的 `hd-kiosk-shell`，tag `v0.2.0` 已打（當時尚未推上 GitHub）。正式交付前要以真正的 tag、從 GitHub 全新 clone 兩個 repo 再走一次 |
| 資料卷 | 實測前 `docker volume ls` 為空，兩個資料卷都是這次才建立的 |
| 設定 | `HD_SHELL_SIGNING=dev`；`MDM_KIOSK_BASE_URL=https://<開發機區網 IP>:18081`（手機要連得到，`localhost` 不行） |

### 9.1 `shell-builder` 的映像檔（Y-04）

| 項次 | 結果 |
|---|---|
| Android 命令列工具、SDK 34、build-tools 34.0.0、Gradle 8.7 | 全部下載並核對 SHA-256 通過 |
| 外殼原始碼 | 從主機上的 clone 取出 `v0.2.0`（`015ee5461349`），版本與契約核對通過，院外網址掃描 0 筆 |
| 預先建置 | **外殼第一次被實際編譯成功**。AGP 另外自動補裝了 platform-tools，一併進了映像檔 |
| 大小 | 映像檔 2.99 GB（Android SDK 與 Gradle 相依占大半） |
| 耗時 | Gradle 那一個檔案（約 130 MB）在開發機的網路上下載了 6.6 分鐘；第一次建置時它卡住超過 15 分鐘沒有進度，**加上重試與「60 秒內低於 1 KB/s 就中斷」之後重建才過**。院內主機的網路若更慢，這一步要預留時間 |

### 9.2 建 APK，第二次不重新產生金鑰（驗收 2）

| 步驟 | 結果 |
|---|---|
| 第一次 `run shell-builder` | 「這一次新產生：院內 CA 伺服器憑證（<IP>） APK 簽章金鑰（開發用）」；**執行時沒有網路**（`network_mode: none`），Gradle 以離線模式建置成功 |
| 第二次 | 三行「沿用既有的……」、「金鑰全部沿用，這一次沒有產生任何新的金鑰」；APK 與簽章憑證的 SHA-256 與第一次相同 |
| `keys-info` 前後各一次 | 輸出逐位元組相同：CA 到 2036 年、伺服器憑證 `IP Address:<IP>` 到 2028 年、簽章金鑰指紋 |
| 改用 `hospital` 執行 | 拒絕：「這個金鑰資料卷是『dev』用的，設定卻是『hospital』……」，結束狀態 1 |
| 資料卷權限 | `ca/`、`signing/` 為 `drwx------ root`；`tls/server.key` 擁有者 1000、`600`；`public/` 有 APK、`index.html`、`shell.json`；容器裡解析不到任何主機名稱 |
| APK 內容 | `tw.hospital.hdcare.kiosk`、versionCode 200、versionName 0.2.0、minSdk 21、targetSdk 34；v2 簽章，簽署者 `CN=HD Kiosk Shell (開發用)`；**嵌入的 CA 與金鑰資料卷裡的院內 CA 逐位元組相同**；權限只有網路、網路狀態、開機完成三項（第四項是 androidx 自動加的、只屬於本 App 的內部權限） |

### 9.3 金鑰備份與還原（DEP-35）

| 步驟 | 結果 |
|---|---|
| `keys-backup` | `backups/keys/hd-keys-<時間>.tar.gz`（約 12 KB），權限 600 |
| `keys-verify` | 四個檔案逐一相符、備份裡的簽章金鑰以備份裡的密碼打得開、CA 私鑰完整 |
| `keys-restore` 到已有金鑰的資料卷 | 拒絕：「金鑰資料卷裡已經有金鑰，不覆蓋……」 |
| `keys-restore` 到一個空的暫時資料卷 | 還原成功並自動核對；`keys-info` 的指紋與原本的完全相同。暫時資料卷事後刪除 |

### 9.4 HTTPS、同一個來源與下載頁（驗收 3 的伺服器那一半）

| 步驟 | 結果 |
|---|---|
| 遷移 | 10 個遷移，套用了 `iteration12_kiosk_shell_version` |
| 啟動 | `patient-web` 記錄「HTTPS；HTTP 只留外殼下載頁」「/api/ 轉送至 http://api:3000」（金鑰先於服務產生，第一次啟動就是 HTTPS） |
| `verify:iteration12 --live` | 全部 ✓：下載頁與 APK（`application/vnd.android.package-archive`）、HTTP 開病人端轉到 HTTPS、只信任院內 CA 就連得上、不信任就連不上 |
| `verify:iteration12:api` 整支**經病人端的 HTTPS 轉送**打進容器裡的後端 | 全部 ✓：版本握手、四種狀態、400、401、稽核 |
| 調高 `KIOSK_SHELL_MIN_VERSION` 到 0.2.1，只 `up -d api` | 契約端點回 `0.2.1`，回報 0.2.0 的平板立刻變成「外殼過舊」；改回空白後恢復「版本正常」（驗收 5） |
| `api` 重新啟動的那幾秒 | 病人端轉送回 502「後端暫時無法連線」，api 起來後自動恢復——平板端的離線佇列本來就要處理這種情形 |

### 9.5 還沒做的

| 事項 | 什麼時候 |
|---|---|
| 開發手機從下載頁安裝、佈建、釘選、跳出、提示列（驗收 3～6 的手機那一半） | 測試分冊 §12.5～§12.7，要實際拿手機做 |
| 以正式 tag、從 GitHub 全新 clone 兩個 repo 再走一次 | 迭代 12 的改動 commit、兩邊 tag 推上去之後 |
| 院內主機上 `shell-builder` 的建置時間與對外連線（`dl.google.com`、`services.gradle.org`、`maven.google.com`、`repo.maven.apache.org`） | 第二次進院前問資訊室（Q-32，網域清單已補在那一題） |

---

## 10. 迭代 13：AI 閘道（0927 本機實測；實驗室待測）

照《[部署手冊](../deployment/index.md)》第十三冊。**分兩半，目前只完成第一半**：

| 半 | 推論端點 | 狀態 |
|---|---|---|
| 本機實測 | 驗收腳本內的**假推論端點**（同時說 Ollama 原生與 OpenAI 相容兩種格式） | ✓ 0927 完成（10.1～10.3） |
| 實驗室實測 | 實驗室 GPU 伺服器上的 Ollama，經 SSH 本機轉送 | **待連線**（10.4 的表格留待回填） |

**本機實測驗得到的**：閘道映像檔、LangChain 兩種方法、`config.yaml` 改檔即生效、後端 `LLM_MODEL_ID` 留空、
真實病人擋在閘道之前、閘道停掉時主線照常——**整條路徑除了模型本身，全部是真的**。
**驗不到的**：真模型的繁中品質與耗時、實驗室的連線方式、個人容器能不能連到 Ollama。

| 條件 | 本次 |
|---|---|
| 程式碼 | 工作目錄（迭代 13 的改動當時尚未 commit） |
| 閘道 | `services/ai-gateway` 建出的映像檔（約 330 MB），以 Docker 執行，只綁 `127.0.0.1` |
| 後端 | 驗收腳本另起的一個，暫存資料庫（全新遷移＋虛構 SOP 文件），`LLM_PROVIDER=lab`、`LLM_MODEL_ID` 空白 |

### 10.1 映像檔與自我測試

| 項次 | 結果 |
|---|---|
| 相依 | 直接用到 6 個（`langchain-core` 1.6.5、`langchain-ollama` 1.1.0、`langchain-openai` 1.6.6、`fastapi`、`uvicorn`、`pyyaml`），連同間接相依共 45 個，全部鎖版本 |
| 建置時的自我測試 | 7 段 24 項全部 ✓，約 3 秒：兩種方法、改檔換模型、推論端點不通回 503／502、逾時回 504、設定改壞回 503 且改回即恢復、管理模型與說明頁的路徑一律不存在、紀錄檔不含提示原文 |
| 執行身分 | `uid=10001(gateway)`，不是 root |
| LangSmith 追蹤 | 映像檔的環境變數明寫關閉 |

### 10.2 端對端（`verify:iteration13:api`，驗收 1～5）

| 驗收 | 結果 |
|---|---|
| 準備 | AI 總開關開得起來——它的前置條件「健康檢查通過」是經閘道成立的 |
| 1 | 衛教產生成功；`ai_invocations` 記下的模型是 `config.yaml` 那一個，**後端沒有設定任何模型名稱**；送到推論端點的內容不含病人姓名與病歷號 |
| 2 | 看似真實的病人：工作失敗、紀錄「已擋下」且不寫提示內容；**假推論端點一個請求都沒收到** |
| 3 | 改 `config.yaml` 的 `model`，下一次呼叫就換成新模型 |
| 4 | 改成 `provider: openai`（OpenAI 相容介面），同樣跑通；後端全程同一個行程 |
| 5 | 閘道停掉：健康檢查不通過、衛教失敗並說明原因、SOP 查詢回「模型未能回答」但原文照列；病人清單、事件記錄與簽核、排班全部照常；閘道再起來之後 AI 自己恢復，後端不必重新啟動 |
| 報告 | `--report` 把四項 AI 功能各跑一次，輸出與耗時寫成 Markdown（本機模式下輸出是假端點的固定回應，只驗報告產得出來） |

### 10.3 實測抓到的問題

| 問題 | 怎麼改 |
|---|---|
| 規格寫「`HttpLlmProvider` 一行都不必改」，但它把 `LLM_MODEL_ID` 當必填、還拿它核對模型清單——換模型就得同時改後端 `.env`，驗收 3 過不了 | 改成可以留空：留空時請求不帶 `model`，由閘道決定並回報 |
| 驗收腳本讓 Docker 隨機配埠，閘道停掉再啟動後換了一個埠，後端接不回去（驗收 5 的最後一項第一次跑是 ✗） | 腳本事先挑好固定的埠 |
| 閘道探推論端點的逾時（5 秒）與後端的健康檢查逾時（5 秒）相同，推論端點慢的時候後端會比閘道先放棄，看到的只是「閘道逾時」 | 閘道的預設改為 3 秒，範本註明要比後端的小 |
| 實驗室規範要求「容器內以 `curl` 確認連線」，但 Python 的精簡映像檔沒有 `curl` | 映像檔裝上 `curl` |

### 10.4 實驗室實測（待回填）

照第十三冊 Z-01～Z-15、[迭代 13 分冊](../testing/iteration-13.md) §13.3～§13.6。**這裡只寫結論，不寫實驗室的位址、埠、帳號**（DEP-42）；
四項 AI 功能的原始輸出留在專案目錄外的報告檔。

| 項次 | 結果 |
|---|---|
| 連線方式（是否要先登入另一台再轉連） | |
| 個人容器：自己建映像檔，或用實驗室指定的映像檔（替代做法） | |
| 容器內連 Ollama 用的是哪一種網路（一般網路或 host 網路） | |
| 選用的模型（主要／備用） | |
| 驗收 1、2（`w25`～`w27`） | |
| 驗收 3、4（`w28`、`w29`） | |
| 第一次呼叫耗時（含載入）／暖機後耗時 | |

| 功能 | 品質（可用／需修改／不可用） | 理由 | 模型耗時 |
|---|---|---|---|
| 個人化衛教內容 | | | |
| 離院衛教重點 | | | |
| 護理記錄草擬 | | | |
| SOP 查詢回答 | | | |

| 收尾 | 結果 |
|---|---|
| 驗收 6：`verify:iteration13 --secrets` | |
| 驗收 7：實驗室個人資料夾以外沒有留東西 | |
| Q-08 仍待補的：使用期限、資料是否被留存 | |

### 10.5 還沒做的

| 事項 | 什麼時候 |
|---|---|
| 10.4 整張表 | 連上實驗室之後（校外連不到，Q-08） |
| 院內那一份閘道在開發機的部署實測上起過一次（`verify:iteration13 --live`：建置、不開埠、`api` 連得到、明確回報設定尚未填好）；院內主機上還沒 | 第二次進院，第十三冊 Z-20～Z-21 |
| 接上正式 GPU API | Q-07 有答案之後，第十三冊 Z-23 |
| 院內主機建置閘道映像檔時的對外連線（Docker Hub、Debian 套件來源、Python 套件來源） | 第二次進院前問資訊室（Q-32，網域清單已補在那一題） |

---

## 版本歷程

| 定版 | 日期 | 異動 |
|---|---|---|
| [0923](https://94sh09sh19sh.github.io/hd-docs/0923/notes/deployment-drills/) | 2026-09-23 | 首次定版。路線 A（現場 `git clone`）的失敗證據、路線 B（離線安裝包）的交付流程、更新與回退各實際演練一次；部署當天要填的欄位另外列出 |

[← 回進度首頁](../index.md)
